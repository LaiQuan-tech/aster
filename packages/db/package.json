{
  "name": "@hr/db",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate": "drizzle-kit migrate",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "apply-sql": "node scripts/apply-sql.mjs",
    "replay": "node scripts/replay-on-pglite.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "drizzle-kit": "^0.31.10"
  }
}
