import { Router } from "express"

/**
 * 員工自改資料審核（employee_profile_change_requests；W6）——**WP0 stub**，端點由 WP8 填：
 *   GET /profile-change-requests?status=、POST /:id/approve、POST /:id/reject {reason}（HR）
 */
export const profileChangeRequestsRouter = Router()
