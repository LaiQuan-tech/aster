import { Router } from "express"

/**
 * 三節／節慶 Cash 獎金（festival_bonuses；M6）——**WP0 stub**，端點由 WP7 填：
 *   POST /festival-bonuses/prepare、GET /festival-bonuses?festival&year、PATCH /:id、
 *   POST /festival-bonuses/pay、GET /festival-bonuses/export.xlsx（HR）
 */
export const festivalBonusesRouter = Router()
