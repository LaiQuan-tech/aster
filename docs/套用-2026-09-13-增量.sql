-- =====================================================================
-- 亞斯特 — 2026-09-13 增量 SQL（勞退自提率）
--
-- 前提：無（不相依 09-12 增量 v2）。但 v2 還沒套的話請一起套，見那份檔頭的四步。
-- 內容：
--   [8] migration 0036 —— salary_structures.pension_voluntary_rate
--       引擎早就會算勞退自提（帳本待辦 #3），只差表上這個欄位。
--       NULL = 不自提；合法值 0–0.06 由 API 把關（法規參數不寫死，故不做 CHECK）。
--
-- 冪等，重複執行無害。驗證見 docs/驗證-2026-09-13-套用後.sql。
-- =====================================================================

-- ─────────────────────────────────────────────────────────────────
-- [8] migration 0036
-- ─────────────────────────────────────────────────────────────────
ALTER TABLE "salary_structures" ADD COLUMN IF NOT EXISTS "pension_voluntary_rate" numeric;
