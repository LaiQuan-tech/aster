# 交接 — 2026-09-22 — 後台測試資料、測試帳號與帳號安全設定

給兩種讀者：(1) **業主**——§2 是「逐頁驗收清單」，每個後台頁面該看到什麼、可以按什麼來試；§3 是測試帳號。(2) **下一個 session**——§6 副作用、§7 seed 工具、§8 清理流程、§9 未完事項。
前一份是 `docs/交接-2026-09-19-後台簡化.md`（9 個分區與網址的由來）。路徑未特別標明者皆相對於 repo 根。

---

## 1. 三十秒版本

- 業主要「後台每個功能各三筆、一眼認得出的測試資料」，好逐頁驗收。今天在**正式租戶**（亞斯特，tenant id `0507ad78-27f4-480e-b99f-a72db2aee50c`）灌了 5 個模組、**55 組 225 筆**【測試】資料（含操作自動產生的通知 20／稽核 18），全部只掛在三位【測試】測試員工身上，可重跑（第二次 created=0）、可一鍵清光（§8）。
- 順手做的四件事：①租戶設定「允許 HR 配發簡單初始密碼」（§4，亞斯特已開）；②員工列表加 Email 欄、操作收成「編輯」＋「⋯」選單（§5.1）；③修一個 bug——出差／零用金核准後預支沒自動開（§5.2）；④併入上游 09-20 技術債批次。
- 網址：後台 `https://aster-system.vercel.app/admin`、前台 `https://aster-system.vercel.app/ess`、API `https://aster-hr-api.vercel.app`。
- 沒有 DB migration、沒有新環境變數；後端只加一個 zod 鍵 `features.accounts.allowWeakInitialPassword`。

### commit 清單（本機 `main`，由舊到新）

| commit | 內容 | 狀態 |
|---|---|---|
| `8cf1224` | seed 基礎：`docs/test/seed-test/lib.mjs`／`run.mjs`／`00-base.mjs`（測試員工與設定檔） | 已 push |
| `0519c70` | 租戶設定 `features.accounts.allowWeakInitialPassword`（設定 → 進階功能 → 帳號安全） | 已 push |
| `7ab5559` | merge origin/main（09-20 技術債批次）；`apps/api/src/routes/tenant.ts` zod 兩邊新鍵都保留 | 已 push |
| `93143d2` | 員工列表加 Email 欄、操作收成選單；新增／重設密碼錯誤改經 `accountErrorMessage` | 已 push |
| `b1c552a` | seed finance（專案與財務分區）＋清理片段 | 已 push |
| `c649cfc` | seed attendance（出勤／假單與簽核） | 已 push |
| `37e0204` | fix：出差／零用金核准時把 `advance_requested` 傳進 `applyApprovalEffects` | 已 push（origin/main 停在這裡） |
| `c1cdb06` | seed payroll（薪資與費用分區）＋清理片段 | **未 push** |
| `c57cd60` | seed people（人員／公告與資訊／系統三區） | **未 push** |
| `b7c30f8` | 清理工具：五個片段組成單一 DO block（含乾跑版）、Storage 清理腳本 | **未 push** |

已 push 的部分（含弱密碼設定與預支修正）已在正式站，兩者都用正式站探針驗過（§4、§5.2）。未 push 的三個 commit 只有測試資料工具與 SQL，不影響線上程式。

---

## 2. 業主逐頁驗收清單

規則：所有測試資料名稱都以 **【測試】** 開頭；員工相關資料只掛在【測試】測試員工 A（主管）／B（正職）／C（兼職）身上。標「（隱藏）」的頁面要先到 **設定 → 進階功能** 勾選該模組，或直接輸入網址（會出現「未啟用」灰色標籤但可用）。

### 2.1 首頁

| 頁面（網址） | 應看到的【測試】資料 | 可以試的操作 | 注意 |
|---|---|---|---|
| 首頁（`/admin`） | 沒有專屬測試資料，但數字卡應反映：「待簽核假單」含 6 張測試 pending 單、「專案示警數」含【測試】專案B／A-變更的 5 種示警、「最新公告」應為【測試】公告 | 點 8 格分區入口進各區 | 「待簽核」有三種定義（§9） |

### 2.2 專案與財務

| 頁面（網址） | 應看到的【測試】資料 | 可以試的操作 | 注意 |
|---|---|---|---|
| 專案（`/admin/projects`） | 專案 4：【測試】專案A（AT-115-001，active，2026-07-01～2027-06-30）、專案B（AT-115-002，active，結束日 2026-08-31 已過）、專案C（AT-115-003，suspended，無合約無期款）、專案A-變更（AT-115-004，變更案，母案 AT-115-001）。成員分潤 9（三個主案各 A lead 50％／B 30％／C 20％）。副委託 3（掛專案A：電機 100,000／空調 80,000／消防 50,000 技師代扣 10％）。專案文件 3（專案A：【測試】文件A／B／C.png） | 開專案A 看成員分潤、副委託、文件；看專案C 的暫停理由；改分潤比例再存 | 專案示警每日通知也會提到【測試】專案 |
| 看板／甘特（`/admin/projects/overview`） | 4 個測試專案（專案B 已逾期） | 切狀態篩選、看時間軸 | — |
| 請款與未收款（`/admin/projects/receivables`） | 期款 5：專案A #1 300,000 已入帳 2026-08-15、#2 400,000 已開票 TEST00003 未收、#3 300,000 未請款（預定 2026-12-01）；專案B #1 250,000 已請款 2026-08-05 未開票、#2 250,000 未請款（預定 2026-09-01 已逾） | 對 A#2 登錄收款；對 B#1 登錄開票；對 B#2 按請款 | 專案C 沒期款是刻意的 |
| 放款作業（`/admin/disbursements`） | 放款單 4：D-115-001【測試】放款A draft（廠商A 50,000，附 PNG 憑證）、D-115-002 paid（廠商B 40,000，2026-08-20，連動副委託空調第 1 期）、D-115-003 paid（其他受款人 10,000，2026-09-10）、D-115-004 void（廠商C 淨額 22,500＋代扣 2,500） | **D-115-001 按付款** | `status=void` 篩選查不到沒 paid_on 的作廢草稿（D-115-004 有帶 paid_on 所以查得到；已開 chip 待修） |
| 年度總覽（`/admin/disbursements/pivot`） | 2026 年應把上面 4 張放款算進廠商A／B／C／其他受款人 | 切年度 | — |
| 獎金季發放（`/admin/bonus-runs`） | 批次 3：【測試】獎金批次A（asOf 2026-08-31，draft，6 項，合計 30,000）、批次B（asOf 2026-09-15，已於 2026-09-20 發放，6 項，30,000）、批次C（asOf 2026-09-30，draft，6 項，合計 0） | **批次A 先「重算」再「發放」**；批次C 看 0 元明細 | 批次A 是在 B 發放前建的草稿，直接按發放會 409 `stale_paid_before`——系統設計，不是 bug |
| 年度總表（`/admin/projects/annual`） | 4 個測試專案的合約／請款／收款彙總（A 合約 1,000,000＋變更 100,000；B 500,000） | 切 2026 年 | — |
| 印花稅（`/admin/stamp-duty`） | 合約 4：【測試】合約A（1,000,000，簽 2026-07-01，已貼 2026-07-05）、報價單A（1,000,000，不適用）、合約B（500,000，簽 2026-05-01，**應貼未貼**）、變更單A（100,000，簽 2026-08-15，已貼 2026-08-18） | 對合約B 登錄貼花 → 示警 `stamp_duty_unpaid` 應消失 | — |
| 專案示警（`/admin/projects/alerts`） | 5 種：專案B 觸發 past_end_date／billing_overdue／unbilled_after_end（high）與 stamp_duty_unpaid（medium）；專案A-變更觸發 no_contract（medium） | 處理其中一項（貼花、請款）後回來看示警消失 | 專案C 是 suspended 所以不觸發 no_contract |
| 客戶（`/admin/clients`） | 3：【測試】客戶A（architect／三聯式／匯款）、客戶B（owner／二聯式／支票）、客戶C（gov／三聯式／匯款） | 編輯、停用 | 沒填統編（有檢查碼驗證） |
| 廠商（`/admin/vendors`） | 3：【測試】廠商A（電機）、廠商B（空調）、廠商C（消防技師），各帶【測試】銀行假帳號 | 編輯銀行資料 | — |
| 公司主體（`/admin/companies`） | 3：【測試】公司主體A／B（帶【測試】銀行）、C（無銀行資料） | 新增、編輯 | 預設主體「亞斯特…」完全沒動 |

### 2.3 假單與簽核

| 頁面（網址） | 應看到的【測試】資料 | 可以試的操作 | 注意 |
|---|---|---|---|
| 簽核（`/admin/approvals`） | 申請單 19：請假 6／加班 3／補卡 3／公出出差 3／零用金預支 4；狀態 pending 6、approved 9、rejected 3、cancelled 1。pending 6 張＝B 假別C 09-10（附 1 張 PNG）、C 特休 09-29、B 加班 09-16、B 補卡 09-17（外出 15:00／返回 16:00）、B 出差 09-21～23（overseas 預估 20,000）、C 零用金 500（09-18） | 切 `?status=pending`：對 **C 的特休單（09-29）按核准** → 假別餘額 C 特休 used 變 8；對 **C 的零用金 500 按核准** → 預支頁應自動多一筆 C 500 requested（§5.2 修的行為）；對 B 假別C 那張先看附件再核准；切 `?status=rejected` 看駁回理由「【測試】駁回示範」 | 這些單第一關簽核人是 A（測試部主管），HR 帳號看到的可能落在「簽核中」而非「待簽核」分頁，HR 可代簽；要走正常路徑就用 test-a 登入前台 `/ess/approvals` 簽 |
| 假別餘額（`/admin/leave-balances`） | 6：特休 2026 A 10／B 7／C 3；【測試】假別A 2026 各 5。B 假別A used 8（09-08 核准） | 核准 C 09-29 特休後回來看 used | used 由核准的假單累加，不要手動改 |
| 月底核銷（`/admin/leave-settlement`） | 2：B 假別A 09-08 已核銷（2026-09）；C 假別B 09-09 可核銷 | **對 C 那張按核銷** | — |

### 2.4 出勤

| 頁面（網址） | 應看到的【測試】資料 | 可以試的操作 | 注意 |
|---|---|---|---|
| 打卡紀錄（`/admin/punch-records`） | A 72 筆／B 69 筆／C 44 筆（2026-08-01～09-21，來源 manual）。特例：B 09-02／09-09／09-16 下班晚 2 小時；B 09-03 下班卡 19:05 是補卡單核准後自動補的；B 09-08 全天假無卡；C 09-09 請假 13–17 → 17:00 才打上班卡 | 依員工／日期篩選；手動補登一筆 | C 是兼職，只排週一三五 13:00–22:00 |
| 出勤月表與月結（`/admin/attendance-sheets`） | 6 張：2026-08 A locked、B approved、C approved；2026-09 A draft、B submitted、C manager_reviewed | **2026-09：對 C 那張（已過主管關）按核准**；B 那張還在等主管 A 審核——用 test-a 登入前台審，或 HR 直接處理（能否跳過主管關以實測為準）；列印月表 | A 的 8 月 locked 是因為 A 的 8 月薪資單已定稿；真實 HR 帳號已收到「出勤月表待審」站內通知 |
| 班表（`/admin/schedules`） | 3 人排班：A 班別A 36 天、B 班別B 36 天、C 班別C 22 天（只排週一三五），範圍 2026-08-01～09-21 | 切 2026-08／09；改某天班別 | 週末與固定假日沒排 |
| 結算作業（`/admin/attendance-settlement`，隱藏） | 6：三人 2026-08、2026-09 各一筆 settled（A 21／15 天、B 21／15、C 13／9） | 重跑 2026-09 看筆數 | 先勾「結算作業」 |
| （無後台頁）補休 | 4：A／B／C 各調整 1h＋C 加班單（payout=補休）核准自動記 1.5h | — | 目前沒有後台頁，只能由 API `GET /comp-time` 看 |

### 2.5 薪資與費用

| 頁面（網址） | 應看到的【測試】資料 | 可以試的操作 | 注意 |
|---|---|---|---|
| 薪資資料與執行（`/admin/payroll`） | 薪資結構 3：A 月薪 45,000＋交通津貼 2,000、投保 45,800、勞退自提 6％；B 按出勤日 36,000／日薪 1,200、投保 36,300；C 時薪 200、週 20 小時、投保 27,470 | 對 B 或 C **逐人**跑 2026-08 薪資作業（帶員工，不要整租戶跑） | 津貼欄位頁面沒顯示（薪資引擎也不讀它）；租戶規則參數沒有保險區塊 → 勞健保自付額算 0，勞退自提仍會算 |
| 薪資單（`/admin/payslips`） | 2026-08 3 張：A finalized（應發 45,000／應扣 2,748／實發 42,252）、B draft（應發 25,200＋代墊 2,060＝實發 27,260）、C draft（應發 21,905.8，含定額補貼 500） | **對 B 按定稿** → B 的 8 月出勤月表會轉 locked | 2026-09 還沒跑 |
| 稅務申報（`/admin/payroll-tax`） | 健保眷屬 3（子女）、扶養親屬 3（子女，2015 年生）、調薪 3（2026-10-01 生效：A 47,000／B 38,000／C 時薪 220）、非員工所得 3（【測試】外部領款人A 20,000 扣繳 2,000 補充保費 422；B 8,000／800／0；C 35,000／3,500／739） | 各分頁新增一筆；看調薪生效日 | — |
| 報銷（`/admin/expenses`） | 費用類別 3（【測試】類別A 實報實銷需憑證、類別B 定額補貼月限額 3,000、類別C 需憑證且需綁出差單）。報銷單 7：2026-08 三張已月結（B 1,200／C 500／B 860）；2026-09：B 類別C 2,400 submitted（綁台中出差單與預支）、C 類別A 300 cancelled、B 類別A 999 rejected、C 類別B 700 submitted。月結核銷 2026-08 1 筆（3 張，實報實銷 2,060、定額補貼 500） | **2026-09 對 C 700 那張核銷**；對 B 2,400 那張核准 | 2026-08 已月結——該期再填單會 409 `period_settled`，請用 2026-09 試 |
| 預支（`/admin/advances`） | 4：B 出差 3,000 paid（匯款）、C 零用金 1,500 settled（balance −1,500）、C 零用金 300 requested、C 零用金 100 requested（§5.2 探針那筆） | 對 C 300 按撥款 → 再核銷 | 在簽核頁核准 C 的零用金 500 後這裡會再多一筆 |

### 2.6 人員

| 頁面（網址） | 應看到的【測試】資料 | 可以試的操作 | 注意 |
|---|---|---|---|
| 員工（`/admin/employees`） | 3 位【測試】測試員工：A（T001／主管／正職／test-a@test.aster.local）、B（T002／正職／test-b@…）、C（T003／兼職／test-c@…）；另有【測試】報到者C（報到完成產生，無登入帳號，Email 欄空白） | **按「⋯」**看選單（My Data／寄重設密碼信／暫時密碼／寄邀請信／停用／異動紀錄）；對測試員工按「暫時密碼」；**新增員工時用業主指定的預設密碼當初始密碼應成功**（§4）；對報到者C 按「寄邀請信」 | Resend 未設：寄信類動作是 dryRun，連結回給 HR 手動轉交，不會真的寄出（§9） |
| 部門與組織圖（`/admin/departments`） | 【測試】測試部（主管 A）＋【測試】測試一組／二組（上層＝測試部）；三位測試員工都掛測試部 | 右側組織圖找到測試部；把一組改掛到二組再改回 | — |
| 到職（`/admin/onboarding`） | 3：【測試】報到者A（2026-10-01，pending）、B（2026-10-05，pending）、C（2026-10-12，completed → 已產生員工列） | 對報到者A 按完成報到 → 員工列表多一位無帳號的【測試】報到者A | 報到完成會自動掛上現行需簽收規章（公告A）的待簽列 |
| 招募（`/admin/recruitment`，隱藏） | 職缺 3（A open 內部／B draft／C closed）、候選人 3（A new／B interviewing／C offered，都在職缺A）、面試 3（2026-10-06～08，面試官 A，結果 pending／pass／fail）、offer 3（draft／sent／accepted，薪資 45,000／52,000／60,000） | 對面試A 填結果；把 offerA 送出 | 先勾「招募」 |
| 績效考核（`/admin/kpi`，隱藏） | 範本 3（各 3 項，權重 40／30／30，滿分 10）；考核 3（2026-Q3）：B draft（reviewer A，範本A）、C submitted（reviewer A，範本B）、A finalized（reviewer HR，範本C） | 對 C 那張按定稿；B 那張用 test-a 登入前台評分 | 先勾「績效考核」 |
| 專屬 Email（`/admin/employee-mail`，隱藏） | 3：A active／B planned／C suspended（test-a／b／c@mail.test.aster.local） | 把 B 由 planned 改 active | 先勾「專屬 Email」 |

### 2.7 公告與資訊

| 頁面（網址） | 應看到的【測試】資料 | 可以試的操作 | 注意 |
|---|---|---|---|
| 公告（`/admin/announcements`） | 3：【測試】公告A（需簽收；A 已讀未簽、B／C 已簽——HR 登錄紙本、報到者C 待簽）、公告B（2 個版本 initial → amendment）、公告C（生效 2026-10-01） | **公告A 看簽收名單**（A pending、B／C signed）→ 對 A 登錄紙本簽署；公告B 看版本歷程；測完可註銷（軟刪） | **全租戶可見，真實員工也看得到**（§6）；設計上員工端只能「查閱」，簽署一律 HR 代登錄 |
| 公司資訊頁（`/admin/company-info`） | 2：【測試】公司福利（benefits）、【測試】職業安全衛生（safety） | 編輯內容存檔；前台 `/ess/company-info` 看 | slug 只有 benefits／safety 兩個（系統寫死），所以只有 2 筆 |
| 知識庫（`/admin/knowledge`，隱藏） | 文件 3：【測試】知識文件A／B／C（文字、已索引、各 1 chunk） | `/admin/knowledge/ask` 問文件內容（需 AI 金鑰有效） | 先勾「知識庫」 |

### 2.8 設定

| 頁面（網址） | 應看到的【測試】資料 | 可以試的操作 | 注意 |
|---|---|---|---|
| 假別與簽核流程（`/admin/leave-types`） | 假別 3：【測試】假別A（有薪、不扣）、假別B（無薪、全扣）、假別C（有薪、**需附件**） | 改假別C 的附件要求再改回 | 簽核流程沒加測試資料（seed 只讀現有流程） |
| 班別（`/admin/shifts`） | 3：【測試】班別A 09:00–18:00、B 10:00–19:00、C 13:00–22:00（休息 60 分） | 編輯 | — |
| 行事曆（`/admin/calendar`） | 3 天：2026-12-28／29／30 標【測試】行事曆A／B／C（dayType workday＝本來就是平日，不改變任何人的出勤） | 切到 2026-12 看 | — |
| 規則參數（`/admin/module-settings`） | 無測試資料（本次沒動） | — | 租戶目前是預設值、沒有保險區塊（見 §2.5） |
| 員工端功能開放（`/admin/module-settings/ess-tabs`） | 無測試資料 | 決定測試帳號登入前台看得到哪些分頁（§3） | — |
| 站台與內部連結（`/admin/company-space`） | 內部連結 3：【測試】連結A／B／C（example.com） | 停用一個再啟用 | 既有連結沒動，只附加 |
| 進階功能（`/admin/settings/advanced`） | 「進階功能」卡：7 個隱藏模組的勾選；「帳號安全」卡：**允許 HR 配發簡單初始密碼＝已開** | 勾「招募／績效考核／專屬 Email／知識庫／人力分析／結算作業」讓上面隱藏頁的分頁出現 | AI 助理沒建測試資料 |

### 2.9 系統

| 頁面（網址） | 應看到的【測試】資料 | 可以試的操作 | 注意 |
|---|---|---|---|
| 報表中心（`/admin/reports`） | 無專屬測試資料；匯出的報表會含【測試】列 | 匯出一份 | — |
| 人力分析（`/admin/dashboard`，隱藏） | 人數統計含 3 位測試員工＋報到者C | — | 先勾「人力分析」 |
| 稽核紀錄（`/admin/audit-logs`） | 18 筆由操作自動產生：tenant_snapshots INSERT ×4、員工專屬信箱 INSERT ×3、報到流程 UPDATE ×1、公告簽收 UPDATE ×2、公告／公告版本 INSERT／UPDATE ×8 | 用【測試】關鍵字或表名篩 | 清理**不刪** audit_logs（稽核保留） |
| 備份快照（`/admin/backups`） | 3：【測試】備份2026-06（69 表／5,125 列／74 檔）、2026-07（6,191 列／75 檔）、2026-08（6,287 列／75 檔） | 看檔案清單、下載 manifest | 是**整租戶真實資料**的快照（備份功能本身如此），存 Storage `tenant-snapshots`；清理預設不刪 |
| 通知中心（`/admin/notifications`） | 20 筆與測試員工相關（給 A 的「待簽核：…」與「出勤月表待審」、給 B／C 的核准／駁回／月表已核准）；真實 HR 帳號另外收到「出勤月表待審」等 | 標已讀 | 清理 SQL 會刪掉掛在測試員工或提到【測試】的通知 |
| AI 助理（`/admin/ai`，隱藏） | 無測試資料 | — | — |

---

## 3. 測試帳號

登入 `https://aster-system.vercel.app/login`，密碼三位相同：`Test-Aster-2026!Seed`（`must_change_password` 已關，不會被強制改密碼）。

| 帳號 | 身分 | 前台可登入什麼、能試什麼 |
|---|---|---|
| `test-a@test.aster.local` | 【測試】測試員工A，主管（manager），T001，正職，2026-01-05 到職，測試部主管 | `/ess/approvals` 簽 B、C 的 6 張 pending 單；`/ess/attendance-sheet` 把自己 2026-09 那張 draft 送出；`/ess/payslips` 看 8 月已定稿薪資單；`/ess/announcements` 公告A（已讀未簽） |
| `test-b@test.aster.local` | 【測試】測試員工B，一般員工，T002，正職，2026-02-02 到職，班別B 10–19 | `/ess/punches` 打卡；`/ess/requests` 看自己 4 張 pending（假別C 附件、加班、補卡、出差）並送新單；`/ess/expenses` 看 2,400 submitted／999 rejected；8 月薪資單 draft |
| `test-c@test.aster.local` | 【測試】測試員工C，一般員工，T003，兼職（週一三五 13–22），2026-03-02 到職 | 同 B；另有 pending 特休 09-29、零用金 500；預支 300 requested；報銷 700 submitted |

注意：
- 前台看得到哪些分頁由「設定 → 員工端功能開放」決定；打卡、送單（請假／加班／補卡／公出／零用金）、看公告、看薪資單、報銷都在前台。
- **A 自己送的單會落到真實 HR 的待簽核**（A 沒有上層主管），seed 才刻意只讓 B、C 送單；用 A 試送單時留意。
- 2026-08 費用已月結，前台填 8 月報銷會被擋（409），用 2026-09。

---

## 4. 帳號安全設定：允許簡單初始密碼（`0519c70`，亞斯特已開）

- **在哪裡**：後台 設定 → 進階功能（`/admin/settings/advanced`）→「帳號安全」卡 →「允許 HR 配發簡單初始密碼」開關。存在 `tenants.features.accounts.allowWeakInitialPassword`；後端 zod 鍵在 `apps/api/src/routes/tenant.ts`，判斷邏輯在 `apps/api/src/services/password-policy.ts`，套用點在 `apps/api/src/routes/employees.ts`；web 端 `apps/web/src/app/admin/settings/advanced/page.tsx`、`lib/auth-api.ts`、`lib/admin-api.ts`。測試：`apps/api/src/__tests__/password-policy.test.ts`、`employees-weak-password-live.test.ts`。
- **影響範圍**：只影響 `POST /employees` 新增員工時帶初始密碼這一條路——API 改成先算 bcrypt `password_hash` 交給 GoTrue `createUser`，因此略過 Supabase 的外洩密碼名單（HIBP）檢查。正式站探針：建帳號帶業主指定的預設密碼 → 201，用它登入 → 200。
- **限制**：
  - 重設密碼**不吃**這條路：GoTrue `adminUserUpdate` 不讀 `password_hash`（回 200 但不生效），所以程式沒走這條。HR 要給簡單密碼請用員工列表「⋯」→「暫時密碼」。
  - 員工自設密碼（首次登入強制改、`POST /me/password`）**仍受檢查**；遇弱密碼目前回 500 而非 422（既有行為，員工端看不到清楚訊息，§9）。
- **目前狀態**：亞斯特租戶＝已開。
- 上游合併（`7ab5559`）時 `tenant.ts` 的 zod 兩邊新鍵都保留，沒有被 strip。

---

## 5. 員工列表改版與預支 bug 修正

### 5.1 員工列表（`93143d2`，`apps/web/src/app/admin/employees/page.tsx`）

- 新增 **Email 欄**：`GET /employees` 經 `listUsers` 反查 auth email；沒有登入帳號的員工（如【測試】報到者C）留空。
- 操作欄只留「編輯」＋「⋯」：選單元件 `apps/web/src/components/ActionMenu.tsx`（桌機下拉、手機 BottomSheet）；項目與顯示／disabled 規則在 `apps/web/src/lib/employee-actions.ts`——My Data、寄重設密碼信、暫時密碼、寄邀請信、停用（紅字）、異動紀錄。
- 新增員工／重設密碼的錯誤改經 `accountErrorMessage` 轉成可讀訊息（不再是原始 code）。

### 5.2 預支沒自動開（`37e0204`）

- 症狀：出差單／零用金預支單核准後，`/admin/advances` 沒有自動出現對應的預支。
- 根因：`decideRequest` 呼叫 `applyApprovalEffects` 時漏傳 `advance_requested`。
- 驗證：正式站探針——對 C 的零用金單核准後預支自動開立（requested 100 那筆就是探針留下的）；seed 的 C 零用金 300 也走同一條路。

---

## 6. 副作用／業主要知道的

1. 【測試】公告A（需簽收）／B／C **全租戶可見**，真實員工登入前台會看到；測完可逐一註銷（軟刪）或跑清理（§8）。
2. 真實 HR 帳號（含維護帳號 `lqtech2026@gmail.com`）收到了「出勤月表待審」等站內通知；專案示警的每日通知也會提到【測試】專案。清理 SQL 會刪掉掛在測試員工或提到【測試】的通知。
3. 2026-08 費用**已月結**：該期再填報銷會 409 `period_settled`，請用 2026-09 試。A 的 2026-08 薪資單已定稿 → A 的 8 月出勤月表 locked。
4. 獎金批次A 是在 B 發放前建的草稿，直接按「發放」會 409 `stale_paid_before`，要先「重算」——系統設計。
5. 預支共 4 筆（含 §5.2 探針留下的 C 100 requested）。
6. 備份 2026-06／07／08 是**整租戶真實快照**（Storage `tenant-snapshots`），清理預設不刪；要刪加 `--backups`。
7. 隱藏模組（招募／績效考核／專屬 Email／知識庫／人力分析／結算作業）的資料有建，要看得先到 設定 → 進階功能 勾選，或直接輸入網址。
8. 公司資訊頁只有 benefits／safety 兩個 slug（系統寫死），所以只有 2 筆。
9. 補休（comp-time）沒有後台頁面，只能由 API 看。
10. 放款列表 `status=void` 篩選查不到沒 paid_on 的作廢草稿（已開 chip，待修）。
11. 三位測試員工是真的 auth 使用者（3 個 `auth.users`），清理會連帳號一起刪。
12. 薪資作業 seed 一律帶 `employeeId` 逐人跑、月結只對 2026-08 跑且跑前確認該期沒有真實員工的單——之後手動試也請照這個規矩。

---

## 7. seed 工具（`docs/test/seed-test/`）

結構：`lib.mjs`（共用層＋規矩）、`run.mjs`（runner）、`00-base.mjs`／`10-finance.mjs`／`20-attendance.mjs`／`30-payroll.mjs`／`40-people.mjs`（五個模組）、`cleanup/`（§8）。每個模組檔頭有「建的東西」與「**與任務導引不同、以實際程式碼為準的地方**」兩段，改東西前先讀。

### 7.1 重跑

```bash
cd /Users/aimand/.gemini/File/aster
SEED_HR_EMAIL=… SEED_HR_PASSWORD=… SEED_TEST_PASSWORD='Test-Aster-2026!Seed' \
  node docs/test/seed-test/run.mjs                         # 全部模組
SEED_HR_EMAIL=… SEED_HR_PASSWORD=… SEED_TEST_PASSWORD='Test-Aster-2026!Seed' \
  node docs/test/seed-test/run.mjs --only finance,people   # 00-base 永遠先跑，--only 只挑其後的模組
```

- `SEED_HR_EMAIL`／`SEED_HR_PASSWORD`：亞斯特租戶的 hr_admin 帳密（例如維護帳號 `lqtech2026@gmail.com`），用 Supabase password grant 換 JWT。`SUPABASE_URL`／`SUPABASE_ANON_KEY`／`SUPABASE_SERVICE_ROLE_KEY` 由 repo 根 `.env` 提供；`API_URL`（或 `--api-url`）預設正式 API。值都不會被印出。
- 安全閘：登入後解碼 JWT 的 `app_metadata.tenant_id`，不等於 `lib.mjs` 的 `TENANT_ID` 就 exit 2，避免灌進別的租戶。
- 跑完印各模組 created／reused 與 manifest 表，並寫 `docs/test/seed-test/last-run.json`——**這個檔在 `.gitignore`，只有本機有**；要看完整 manifest 就重跑一次（冪等，不會多建）。

### 7.2 冪等證據

最後一次全模組重跑（`last-run.json`，ranAt 2026-09-21T19:34:47Z）：status ok、issues 0、**created=0、reused=207**（base 33／finance 51／attendance 51／payroll 30／people 42）。manifest 55 組 225 筆（207 筆 seed 建的＋通知 20／稽核 18 是讀回的）。

### 7.3 加新模組

契約在 `lib.mjs` 檔頭與 `createContext()`：
- 新檔 `NN-name.mjs`（兩位數前綴決定順序），export `name` 與 `seed(ctx)`；runner 依檔名載入。
- 四條規矩：(a) 名稱一律 `T("…")` 加【測試】前綴；(b) 員工相關資料只掛 `TEST_EMPLOYEES` 三位（`ctx.state.employees.A/B/C`）；(c) 冪等——先 GET 列表比對名稱／email／code，有就沿用（`ctx.ensure()`），狀態機類先讀現況再推進；(d) 同時寫 `cleanup/NN-name.sql` 片段並加進 `cleanup/build.mjs` 的 `ORDER`（子表先、父表後）。
- 不確定能不能做的事用 `issue()` 記進 manifest，不要吞掉。
- 跑兩次確認第二次 created=0，再把 manifest 那段抄進交接文件。

---

## 8. 清理流程

順序固定，**先 Storage 後 SQL**（SQL 要先從 DB 讀 `storage_path`，DB 先清就找不到檔案路徑）：

```bash
cd /Users/aimand/.gemini/File/aster
node docs/test/seed-test/cleanup/storage.mjs --list      # 只列出，不刪
node docs/test/seed-test/cleanup/storage.mjs             # 刪 Storage 檔（目前 6 個：放款憑證／專案文件／假單附件／報銷收據）
node docs/test/seed-test/cleanup/storage.mjs --backups   # 才會連 2026-06／07／08 三份備份快照一起刪（預設不刪）
```

然後把 `docs/test/清理-後台測試資料.sql` 拿去 Supabase **Management API query 端點或 SQL Editor** 由人跑（AI 不直接對正式 DB 下破壞性 SQL）：
- 單一 `DO` block、單一交易：先把租戶切成 demo → 刪 → 切回 active，任一句失敗整包回滾；可重複執行（第二次全部 0 列）。
- 片段順序 `30-payroll → 20-attendance → 10-finance → 40-people → 00-base`（payroll 的報銷綁預支／出差單所以先於 attendance；其餘都掛在測試員工身上所以 base 最後刪員工與帳號）。
- **乾跑證據**：`node docs/test/seed-test/cleanup/build.mjs --dry-run` 產生的版本（結尾 `RAISE EXCEPTION`，看到 `DRY_RUN_ROLLBACK` 即代表每句都能過）已於 09-22 在正式租戶跑過：全部語句可執行、刪完【測試】相關列全 0、真實員工 21／真實假單 2／真實打卡 29 不動。
- **不刪**：真實員工與他們的任何資料、`audit_logs`（稽核保留）、Storage 備份快照（除非 `--backups`）。
- 若之後 seed 新增資料種類：改對應 `cleanup/NN-*.sql` 片段後重新 `node docs/test/seed-test/cleanup/build.mjs`（正式版）與 `--dry-run`（乾跑版 `docs/test/清理-後台測試資料.dry-run.sql`），乾跑先過再用正式版。
- 只想拿掉公告、不想全清：後台公告頁註銷（軟刪）即可。

---

## 8b. 正式站逐頁驗收結果（2026-09-22，fresh-context agent，全程唯讀）

53 個頁面／視圖全部開過，每頁都看得到對應的【測試】資料，API 全部 2xx、console 無 JS 錯誤。
順手抓到並已修掉（commit `7cd5e8a`，已部署）：

- **打卡紀錄／出勤月表明細／報表中心明細／面試時間顯示成 UTC**（比台北少 8 小時；`/admin/punch-records`
  把 08:59 顯示成 00:59，真實員工的打卡也受影響）→ 改用 `ess-format` 的當地時區格式。
- **簽核頁「零用金預支」類型欄空白、無法篩選**（`RequestKind` 型別漏了 `petty_cash`）→ 補標籤與篩選選項。

看到但屬設定或設計、沒有動的（業主決定）：

- 2026-08 薪資單勞保／健保自付額都是 0：租戶規則參數沒有設勞健保費率（設定 → 規則參數 → 勞健保）。
- 測試員工的假別餘額出現負數（給假 5 用 8）：系統目前允許超用；要不要在送單時擋是規則決策。
- 行事曆 2026-12 的【測試】標籤只在滑鼠懸停的 tooltip 出現，工作日標籤不直接顯示。
- 公告C 生效日是 2026-10-01，但員工端現在就看得到（生效日目前只是資訊欄位）。
- 多處狀態／類型欄顯示英文原值（假別餘額的 pending/rejected、報銷的 submitted/settled、報表的
  business_trip、結算作業的 workday、招募的 regular、通知中心的 attendance_sheet 與收件人 UUID）。
- 稽核紀錄有大量「系統／排程 更新員工（無欄位變動）」的觸發器噪音；seed 重跑時的
  `POST /attendance-sheets/generate` 呼叫也會留下紀錄。
- 年度總表把消防「技師簽證費」放在備註欄而非消防欄。

## 9. 未完／待業主決定

本次新增：
1. **重設密碼也要吃弱密碼？** 需要 SECURITY DEFINER 函式直接改 `auth.users.encrypted_password`（動 Supabase auth schema），要業主拍板才做；目前替代方案是「暫時密碼」。
2. `POST /me/password` 遇 `weak_password` 回 500 而非 422（既有行為），員工端看不到清楚訊息——要不要修。
3. 放款列表 `status=void` 篩選查不到沒 paid_on 的作廢草稿（chip 已開）。
4. 補休沒有後台頁面。
5. `c1cdb06`／`c57cd60`／`b7c30f8` 與本文件尚未 push（截至本文件 commit 時本機 main 領先 origin/main）。

前次交接仍有效：
6. **金鑰輪替仍未做**：`sbp_`（Supabase）、`vcp_`（Vercel）、Railway、Gemini 四把都曾貼進對話，舊值視為已外洩；本文件刻意不重複任何值。
7. **Resend 未設**（`RESEND_API_KEY`）：邀請信／重設密碼信一律 dryRun（連結回給 HR 手動轉交），通知不寄 email。
8. **三種「待簽核」定義不一致**（09-19 交接 §9.8）：側欄徽章＝輪到我簽的、簽核頁「待簽核 N」＝含未指定簽核人的、首頁卡＝全租戶 pending 總數；對不上不是 bug，待業主決定要不要統一。
9. 09-19 的 fresh-context 瀏覽器驗收清單（桌機側欄／手機抽屜／簽核頁五種 status…）仍未做——這次的測試資料正好可以拿來跑。

## 10. 同日下午追加（皆已部署）

- **管理員設定員工密碼**（`b2a5da2`＋DB `packages/db/sql/0038_auth_set_user_password.sql`，已套正式庫）：員工列表「⋯」→「設定密碼」，可自填或一鍵產生；租戶開著「允許簡單密碼」時走 `auth_set_user_password`（SECURITY DEFINER 直接寫 `auth.users.encrypted_password` 並清掉該使用者所有 session，只給 service_role），關著時走 GoTrue（受外洩名單檢查）。成功後 `must_change_password=true`，稽核 `reason: hr_set_password`。
- **批次匯入改成 Excel 範本**（web `3eba193`／`e251936`，api `d78089d`）：`GET /imports/:kind/template`、`POST /imports/:kind {fileName, dataBase64, dryRun, options}`，六種 kind：punches／schedules／salary-adjustments／onboardings／employees／holidays。範本用中文表頭、工號＋姓名、班別名稱、台北時間；附「說明」「員工清單」「班別清單」「部門清單」工作表。面板：下載範本 → 上傳（先 dryRun 預檢，逐列錯誤）→ 確認匯入（只匯沒錯的列）。舊的 `{csv}` 端點保留、改呼叫同一組 writer。上限 4MB／5000 列；employees kind 每列打 auth API，幾百列可能碰 Vercel 60 秒。正式站已驗：範本可下載、原樣上傳 dryRun 回 `total 2 / valid 0`（範例列提醒）。
- **Apollo 字眼全部移除**（`0b2e468`，49 處 29 檔）。
- **打卡紀錄**：測試員工的打卡已依業主要求刪除（186 筆），月表與結算結果仍保留舊算值；別再跑 `--only attendance`。

