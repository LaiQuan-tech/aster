# 亞斯特 ERP — 專案帳本

> 本 session 無 Task* 工具，帳本以檔案維護。每次 session 開場先讀這份。
> 建檔：2026-08-24

## 決策紀錄

| 日期 | 決策 | 理由 |
|---|---|---|
| 2026-08-24 | 自行開發（非外包規格書） | 使用者裁示 |
| 2026-08-24 | 資料模型先行、實作分階段 | 專案/請款/獎金是同一組資料的三個投影，切碎做會重工 |
| 2026-08-24 | **不實作 C 單拆卡機制** | 偽造出勤紀錄；系統化後留下完整證據鏈，風險高於現行人工作業。改以「工時預警 + 紀錄真實/給付管道分流」覆蓋需求 |
| 2026-08-24 | 薪資模組採平行雙軌 2 個月才切換 | 算錯錢＝信任崩盤，屬 judgment.md 第1條 (a) 靜默污染 |
| 2026-08-24 | 跨公司金流＝**集團代收代付**（使用者確認） | 據此加建區塊 L：往來款流水、沖抵、帳齡 view |
| 2026-08-24 | 開票公司 ≠ 簽約公司時「標記不阻擋」 | 該情況已非代收代付；不擋流程（實務有合理情境），但要在報表可見 |
| 2026-08-25 | 薪資制度＝**月薪 + 外包承攬**（使用者更正，非時薪） | 薪資引擎只需一條月薪路徑；外包走 9B 執行業務所得 |
| 2026-08-25 | **承攬人員不進打卡/請假/薪資模組** | 出勤紀錄是「受指揮監督」的證據，會成為假承攬真僱傭的證物 |
| 2026-08-25 | 技術棧：Vite 6 + React 19 + TS + Supabase + Tailwind 4 | 延續 mazu-beta 慣例；新增 Tailwind（ERP 表單密集）與 router |
| 2026-08-25 | 核心計算寫在 PostgreSQL function，不寫在前端 | 可搬移到任何 PG（含公司內網自架），Supabase 只當 auth + hosting |
| 2026-08-25 | 驗證用 pglite（WASM PG），不依賴 Docker | 本機無 Docker/postgres；migration 與引擎皆可實跑驗證 |

## 進度

- [x] 需求評估、指出規劃書失真點（語音轉錄「技師→祭師」）
- [x] 資料模型 v0（docs/02-資料模型-v0.md）
- [x] 關係企業性質確認（集團代收代付）→ 區塊 L 已建
- [ ] **取得會議逐字稿** ← 目前最大阻塞，見 01 檔待確認清單
- [ ] 資料模型 v1（逐字稿校正後）
- [x] 技術棧決策（Supabase + PG function，保持可搬移到內網）
- [x] **DB 層完成**：37 張表 / 32 個 function，40 項功能測試通過
  - [x] 出勤打卡（GPS 圍欄、補登審核、工時分段）
  - [x] 請假特休（週年制自動發放、餘額流水帳）
  - [x] 薪資引擎（月薪、加班費、勞健保、平行雙軌）
  - [x] 外包承攬給付（9B 扣繳起扣點、補充保費）
  - [x] 獎金（批次、比例分配、裁示調整留痕、歷史對比）
  - [x] 稽核軌跡與 RLS 權限分層
- [ ] **法規參數查證**（會計核對）＋兩張表匯入：投保級距、扣繳稅額表 ← 阻塞上線
- [ ] 前端：手機打卡 PWA
- [ ] 前端：補登審核、請假簽核
- [ ] 前端：薪資試算與雙軌比對
- [ ] 前端：獎金分配與發放
- [ ] 專案/請款/獎金連動模組（客製化階段）

## ⚠️ 2026-08-26 重大轉向：改以既有 repo 為基礎

使用者提供 https://github.com/LaiQuan-tech/aster —— 經檢視，**這不是範例，
是已經在為亞斯特開發中的實際系統**（2026-07-27 commit 將系統名改為
「亞斯特設計顧問 數位化系統」，8/19 仍在補 RLS），已部署 Vercel + Railway。

規模：41 張表、40,048 行 TS/SQL、25 個測試檔、26 個 migration。
技術棧：Turborepo monorepo / Next.js 16 + Express 5 + BullMQ / Drizzle + Supabase RLS。

**決策：以該 repo 為開發基礎。本專案（aster-erp）的 migration 停止開發。**
理由：對方覆蓋更廣（多排班、簽核流程、KPI、招募、通知）、引擎品質更高
（規則驅動純函式、golden test 已對過客戶既有薪資表）、且已上線。
本專案的 SQL 是平行造輪子。

### 本專案仍有效、應移植過去的部分

| 項目 | 對方狀態 | 來源 |
|---|---|---|
| 稽核軌跡 audit_logs | **完全沒有** | 008_audit_rls.sql |
| 關係企業代收代付 + 帳齡 | **0 個檔案** | docs/02 區塊 L |
| 分期請款 / 發票 / 入帳 | **0 個檔案** | docs/02 區塊 F |
| 合約版本 + 印花稅追蹤 | **0 個檔案** | docs/02 區塊 E |
| 雇主負擔成本（勞退6%、雇主勞健保） | 引擎未計算 | 006_payroll_engine.sql |

docs/01（C 單否決理由、代收代付邊界）與 docs/02（模型分析）不受影響，仍為有效分析。

### 在對方程式碼發現的疑似 bug（待確認）

`packages/rules/src/payroll-engine.ts:191` 計算健保自付額時用
`(1 + (salary.nhiDependents ?? 0))`，**未裁切眷屬 3 口上限**；
同 repo 的 `tw-tax.ts:108` `nhiEmployeePremium()` 有正確裁切，且其測試
明確驗證 5 口應以 3 口計。兩條路徑邏輯不一致，實際算薪資單走的是前者。
若寫入 nhiDependents 前未於他處裁切，眷屬 4 口以上員工每月會被多扣健保費
（30,000 投保、5 口 → 多扣約 930 元/月）。
※ 未完整追查 API 層寫入路徑，需確認後再定論。

## 2026-08-26 aster repo 實跑狀態

已 clone 至 `~/Documents/coding/aster`（完整 clone，可推送）。

| 項目 | 結果 |
|---|---|
| npm install | ✓ 成功（14 個 vulnerabilities 待評估） |
| typecheck | ✓ 5/5 全過（修 turbo 依賴後 clean clone 可直接跑） |
| @hr/rules 測試 | ✓ 41 項全過（golden 23 + tw-tax 14 + rules-schema 4） |
| @hr/db 測試 | ✓ 12 項全過 |
| @hr/api 測試 | ✗ 20 檔載入失敗——整合測試需真實 Supabase 憑證 |
| web 前端啟動 | ✓ 登入頁正常渲染（Next.js 16.2.9 Turbopack） |
| 登入看系統內部 | ✗ 需真實憑證；demo 帳號亦走 Supabase Auth |

### 本輪修掉的三個問題（皆影響新環境，尚未 commit）

1. **turbo.json 缺 `dependsOn: ["^build"]`**（test/typecheck）
   → clean clone 跑 typecheck 會噴 10 個 `Cannot find module '@hr/rules'`，
     因為 rules 的型別在 dist/。CI 或新開發者必踩。
2. **apps/web 未載入 repo 根 .env**
   → apps/api 與 apps/worker 都有用 dotenv 載根目錄，只有 web 漏了。
     線上靠 Vercel 注入環境變數所以沒現形，本機開發則前端直接白畫面
     （client 端 `supabaseUrl is required`）。已於 next.config.ts 補上，
     並用 `process.cwd()` 而非 `import.meta.url`——next.config.ts 會被編成 CJS。
3. **缺 .env.example**（.gitignore 原已預留 `!.env.example` 例外，但檔案不存在）
   → 已補，含全部 24 個環境變數與用途說明。

### 阻塞：需要真實 Supabase 憑證

`~/Documents/coding/aster/.env` 目前是 placeholder。需由使用者自行填入真實值
（**不要貼進對話**，service_role key 可繞過 RLS）。

## 2026-08-26 重新部署上線

README 指的兩個網址都是錯的：`hr-theta-peach`（更名前 HRLink 版本）與
Railway API（服務已不存在，404）。實際專案是 Vercel 上的 aster-system / aster-hr-api，
且**最近一次部署 15 天前為 Canceled，線上實跑的是 26 天前（約 7/31）的版本**，
缺 8/07 薪資引擎修正與 8/19 RLS 資安修補。

已從本地重新部署兩者至 production：

- Web: https://aster-system.vercel.app
- API: https://aster-hr-api.vercel.app/health → `{"status":"ok"}`
- Supabase: jpbtwvxwjrjspkiinrvy.supabase.co（存活，回 401 要 apikey 屬正常）
- 前端 NEXT_PUBLIC_API_URL 已正確指向 aster-hr-api.vercel.app

部署內容 = main HEAD + 下列未 commit 的本地修改（線上與 git 暫不一致，待 commit）：
`turbo.json`、`apps/web/next.config.ts`、`apps/web/package.json`、
`.env.example`(新)、`.gitignore`(加 .vercel)、`README.md`(線上環境網址更正)。

## 2026-08-26 找到並修正薪資 bug（比原判斷嚴重）

原本懷疑的是「健保眷屬未裁切 3 口上限 → 多扣」，追下去發現是更根本的斷鏈：

`apps/api/src/routes/payroll.ts` 的 `SalaryRow` / `toSalaryStructure` 只映射
method / baseSalary / dailyWage / hourlyWage，**未帶入 labor_insured_salary 與
health_insured_salary**。引擎的保費計算是 `ins && salary.laborInsuredSalary ? ... : 0`，
欄位缺漏時靜默走 0 → **每張薪資單的勞保費、健保費都是 0，實發金額被高估**。

鏈上其他環節都在（寫入端 routes/salary.ts、DB 欄位、引擎計算、rule_config 的
insurance 設定），只有讀取端 mapping 沒接上。以投保 38,200 計，每人每月少扣
955 + 592 = 1,547 元。眷屬則是完全沒計入。

### 修正（commit e01516b, d25f9dc）
- payroll.ts: 補兩個投保薪資欄位的 select 與映射
- payroll.ts: 查 nhi_dependents（insured=true）算眷口數帶入引擎
- payroll.ts: 回傳 `missingInsuredSalary`，列出有保費規則卻未填投保薪資者
- payroll-engine.ts: 健保改走 tw-tax 的 `nhiEmployeePremium`，法定 3 口上限
  裁切邏輯只留一份（原先兩處實作不一致）
- 新增 payroll-engine.test.ts 8 項回歸測試

驗證：typecheck 6/6、rules 49 項（原 41 + 新 8）、db 12 項全過、build 4/4。
API 已部署 production，health 回 ok。已 push（2026-09-10）。

### 仍存在的缺口
`salary_structures` 無勞退自提率與預支欄位 → `pensionVoluntaryRate` /
`advance` 仍無資料來源，需另加 migration。

## 實跑驗證紀錄

- 2026-08-25 `npm test`：8 個 migration 全通過，40 項功能測試全通過。
  驗證環境為 pglite（WASM PostgreSQL），非 Supabase 實機——
  RLS policy 的實際行為需在 Supabase 上以真實 anon/authenticated 身分再驗一次。

## 未決事項

見 `01-需求評估與待確認.md` 的待確認清單（10 項）。

---

## 2026-09-10 換機器交接

程式碼與文件全部在 GitHub（`LaiQuan-tech/aster`，main）。原 `aster-erp/`
目錄不在任何 repo 底下，內容已收進本 repo `docs/設計參考/`。

**新 session 接手順序**：本檔 → `01-需求評估與待確認.md` → `aster/README.md`。

### 待辦（依急迫度）

1. ~~**repo 是 public**~~ **已完成（2026-09-11 確認 visibility=private）。**
   金鑰未曾進過 git（全歷史掃過，只有 .env.example 佔位符），毋須輪換。
   ⚠️ 但同組織下 `LaiQuan-tech/aster-pitch` 仍為 public，內容未查，需確認
   是否含本專案資料。
2. **dependabot 29 個漏洞**，4 個 critical（next、vitest）。上線前要處理。
3. `salary_structures` 缺勞退自提率、預支欄位 → 需 migration。
4. RLS policy 只在 pglite 以 owner 身分驗過，等於未驗；需以真實
   anon/authenticated 身分在 Supabase 重驗。
5. 查 Vercel Git integration 為何停止追蹤 main（8/19 的 RLS 修正曾兩週未上線）。
6. 移植五個缺口：audit_logs、集團代收代付、應收開票/收款、合約版本與印花稅、
   雇主成本試算。

### 客戶 25 條開發規則

使用者將依逐字稿拆成 25 條規則逐條討論。第 1 條（2026-09-10）：

> 打卡上限與拆單邏輯：正常出勤卡累計滿 40 小時需自動鎖定/阻擋。超過 40 小時
> 之加班或在家工作時數，系統需自動拆單並轉入隱藏版 Cash 卡（C 單/C 版）紀錄

**處置：拆兩半。**
- 前半（40 小時上限與告警）**做**。§32 月加班上限 46 小時（工會同意可 54，
  每三個月 138 封頂），40 當內部緩衝合理。做成 `work_hour_alerts`，
  36/40/46 三段告警，擋的是再派新工，不是擋紀錄。
- 後半（自動拆單轉隱藏 C 卡）**不做**。理由見
  `01-需求評估與待確認.md`。補充論點：若 40 是每週正常工時（§30 的數字），
  拆完後全公司出勤紀錄都會停在剛好 40 小時、零加班——這種整齊本身就是紅旗。
- 替代方案（已在模型內）：出勤照實記 + `overtime_settlements` 獨立給付紀錄，
  可含現金管道與自己的稅務處理。帳面誠實、錢照付。

**逐字稿仍未取得。** 使用者表示 25 條是依逐字稿拆的，已再次請求原文，
特別是第 1 條——「隱藏版 Cash 卡」是整理後的說法，若原話指的是外包／
非僱傭關係的獨立給付管道，性質完全不同，是可以正常做的。

### 第 2 條（2026-09-11）

> 三階段打卡紀錄機制：系統需支援同天多次打卡邏輯，第一與第二次打卡為正常
> 上下班紀錄（供勞動局檢查）；第三次或超額打卡自動隱藏並將數據帶入 C 單系統中

**處置：同樣拆兩半，但前半「已經做完」，後半的否決理由比第 1 條強。**

#### 前半（同天多次打卡）— 不是新需求，現況已支援

本輪實際讀碼確認，非推測：

- `apps/api/src/routes/punch.ts`：`POST /punch` 對單日打卡次數**無任何上限**。
  省略 `type` 時依「今日最後一筆 in/out」推斷，第 3 刷自然回到 `in`。
- `apps/api/src/services/settlement.ts` `pairPunches()`：依時間排序走訪當日
  **全部**打卡，逐對配成 in/out 區間（4 刷→2 段、6 刷→3 段），
  落單的計入 `anomaly.unpairedPunches`。
- `packages/rules/src/worktime-engine.ts` `computeAttendanceDay()`：
  簽章即 `PunchPair | PunchPair[]`，多段相加，註解寫明
  「一個 in/out 對,或多段 (含中離)」。

#### 後半（第 3 刷以後自動隱藏 + 帶入 C 單）— 不做

第 1 條寫「隱藏版 Cash 卡」，字面上還能解讀成給付管道。第 2 條寫明
「第一與第二次打卡為正常上下班紀錄（**供勞動局檢查**）」——
指定了觀眾（勞動檢查員）與動作（自動隱藏）。這不是給付管道的問題，
是對主管機關呈現不實紀錄。落在硬約束第 1 條「不實作出勤紀錄造假機制」，
不再逐案評估。

第 1 條已寫的理由不重複，本條補三點：

1. **本條與第 1 條的判準互斥，同時實作會打架。**
   第 1 條藏的是「累計逾 40 小時的部分」（時數門檻）；本條藏的是
   「第 3 刷以後」（次數門檻）。同一天可以滿足其一而不滿足其二，
   兩套隱藏規則疊在同一批 `punch_records` 上會產生互相矛盾的「可見紀錄」。
   這種不一致本身就是轉錄稿被逐字當規格的徵兆。

2. **兩刷上限會把正常工時也藏掉，方向是反的。**
   午休外出是 4 刷（09:00 in／12:00 out／13:00 in／18:00 out）。
   只留前兩刷，可見紀錄變成「09:00–12:00，當日 3 小時」。
   這不是藏加班，是把下午整段**正常工時**藏掉——對勞檢反而成為
   工資未全額給付的證據，對員工是少算薪。外出洽公、跨日班同理。
   想藏加班的需求，用次數門檻在技術上根本達不到。

3. **「隱藏」在資料庫裡不是刪除，藏不掉。**
   自動隱藏＝加旗標或搬另一張表，原始 `punch_records` 仍在（且本表設計
   為 append-only）。勞動檢查與訴訟中的文書提出命令都調得到；
   而拆單規則與 C 單對照表本身會證明這是設計出來的，不是漏記。

#### 替代方案：顯示分層，不是紀錄隱藏

推測真實痛點是「HR 看報表時中離／外出的打卡把畫面弄亂」。若是如此，
要的是預設視圖收斂，不是紀錄隱藏：

- 報表預設一行摘要「當日首刷／末刷／合計工時／段數」，展開才看全部打卡明細。
  紀錄 100% 完整、逐日至分鐘，勞檢時展開即可。
- 若真實需求是加班費走現金：走第 1 條已定案的 `overtime_settlements`。

**待確認（同第 1 條，仍缺逐字稿）**：「供勞動局檢查」是客戶原話還是整理稿
的推論？若原話只是「報表預設只看上下班兩刷」，那本條前後半都沒有爭議，
純粹是 UI 需求。

#### 本輪在程式碼發現的三個缺口

1. **`break_in` / `break_out` / `outing_in` / `outing_out` 四種打卡形同虛設。**
   `punch.ts` 的 zod schema 收得進去、`punch_records` 存得下，但
   `pairPunches()` 只認 `in`/`out`，其餘型別靜默略過（不配對也不計入 unpaired）。
   工時扣除走的是班別上的固定 `shift.breakMinutes`
   （`worktime-engine.ts`：`workedMinutes = grossMinutes - shift.breakMinutes`）。
   → **外出 2 小時的人，工時不會變少。**
   這是「同天多次打卡」目前唯一真正沒接上的一段，也是本條前半唯一該做的工。
2. **多刷路徑無回歸測試。** `punch.test.ts` 只測到第 1、2 刷與權限隔離，
   沒有第 3 刷以後、也沒有多段結算的測試。
3. **第 1 條的替代方案尚未動工。** `work_hour_alerts` 與 `overtime_settlements`
   在本 repo **不存在**，只在 `docs/設計參考/migrations-superseded/002_attendance.sql`
   （已停止開發的 aster-erp）。

> 法條引用（勞基法 §30 V/VI 逐日記載至分鐘、保存 5 年）沿用 docs/01 既有分析；
> 舉證責任與罰則的確切條號金額待律師／會計師覆核，不在此臆斷。

### 第 3 條（2026-09-11）

> 在家工作（WFH/加做）彈性申報：提供「在家工作/加做」勾選與申請欄位，
> 開放同仁自主申報[2]；加做時數扣除後未超標按正常計算，超標部分自動轉 C 單處理

**處置：前半做（三條裡第一個真正該開發的），後半不做。**

> 註：原文「自主申報**[2]**」帶引註編號，再次確認手上是**有註腳的整理稿**，
> 非逐字稿。該 `[2]` 指向的來源值得索取。

#### 前半（WFH 自主申報）— 要做，且地基已齊

現況：全 repo 無 `wfh` / `remote` / 「在家」任何字樣，這個概念不存在。
但申請簽核的地基剛好完整：

- `apps/api/src/routes/requests.ts`：`KINDS = ["leave","ot","fix_punch","business_trip"]`，
  已有多關簽核（approval-flows）、多段日期（`segments`，最多 31 段）、
  代申請（`onBehalfOfEmployeeId`）、附件（request_attachments）。
- `business_trip` 是現成範本：走同一條簽核管線，但**不動帳**
  （`services/ledger.ts` 的 `applyApprovalEffects` 只處理 `leave` / `ot`），
  核准即視為已授權。

→ 新增 `wfh` kind 是小工，不是新系統。

**但 WFH 與 `business_trip` 有一處關鍵差異：WFH 時數必須動帳。**
在家工作的時數就是工時，要進同一張 `attendance_days`，
不能像 `business_trip` 那樣只標記核准。建議做法：核准後產生
`source='wfh'` 的打卡對餵進 settlement，與現場打卡走同一條結算路徑。

**自主申報必須配核定關卡——這是保護雇主，不是限制。**
若「開放自主申報」沒有核定，風險方向與客戶擔心的相反：員工自報 100 小時
加班，雇主須照付。加班的前提是雇主同意／指派。既有 approval-flows 直接套用。
另：勞動部《勞工在事業場所外工作時間指導原則》對遠距工作即採
「勞工自行記載＋雇主記載」，**自主申報是法定建議做法**，不是灰色地帶。

#### 後半（超標自動轉 C 單）— 不做

同第 1、2 條，理由不重複（此為 C 單尾巴第三次出現，見下方「流程調整」）。
本條補兩點新論據：

1. **這會讓 §32 月上限依設計永遠不觸發，使第 1 條的告警系統失效。**
   第 1 條處置要做 `work_hour_alerts`（36/40/46 三段）。若超標時數自動轉入
   另一個池子，46 小時這條線**結構上永遠達不到**——告警變成裝飾品。
   同一模組內的兩個功能互相抵銷。
   ※ 本輪另確認：`packages/rules/src/rules-schema.ts` 的 `OvertimeSchema`
   僅 `{ rules: OvertimeRuleSchema[] }`（條件式倍率），**整個規則引擎沒有
   任何月上限維度**。§32 的 46/54/138 目前不存在。第 1 條的缺口比原記載更大：
   不只 `work_hour_alerts` 表未建，是 schema 本身缺這個維度。

2. **在家工作發生職災仍屬職災。** 時數未記載，職災認定與勞保給付即生爭議，
   而爭議時舉證不利者為雇主。藏 WFH 時數，正好在最需要紀錄的場景
   拆掉雇主自己的防線。

**替代方案**：WFH 時數照實進 `attendance_days`；超標部分走第 1 條已定案的
`overtime_settlements`（可含現金管道與獨立稅務處理）。與第 1 條同一個答案。

#### 待確認（阻塞資料模型，需逐字稿或問客戶）

1. **「加做」是什麼？三種可能，資料模型完全不同：**
   - (a) 加班的另一種說法 → 就是既有 `ot`，不需新東西
   - (b) 額外承接的專案工作量（設計顧問業常見）→ 屬專案／請款模組，非出勤
   - (c) 為關係企業做的工 → 踩到區塊 L 代收代付＋假承攬，完全不同題目
2. **「扣除後未超標」的「扣除」扣什麼？** 扣休息時間？扣已達正常工時的部分？
   原文未言明。
3. `[2]` 註腳指向的原始來源。

#### 可立即動工的範圍（待使用者確認）

`wfh` request kind：requests.ts 加 KIND、核准後產生 `source='wfh'` 打卡對、
settlement 照常結算、punch_records 的 source enum 加 `wfh`、對應測試。
不含「加做」——語意未定前不建模。

---

## 流程調整（2026-09-11）

C 單尾巴已在第 1、2、3 條連續出現，判準各不相同（時數門檻／次數門檻／
超標門檻）且互相矛盾。**這是同一個決策被整理稿拆成多條重述，不是三個需求。**

後續規則若再出現「自動轉 C 單／隱藏／拆單」尾巴，**直接引用第 1 條處置，
不再重寫否決理由**，把討論時間集中在各條的前半實質需求。
若某條的尾巴出現真正新的性質（例如明確指向非僱傭關係的獨立給付管道），
再單獨評估。

### 第 4 條（2026-09-11）

> 特休與請假自動扣減（週年制）：請假與特休採「週年制」（依員工到職日計算）[7]。
> 同仁於系統提出請假申請經主管審核（approve）後，系統自動扣減剩餘假數並開放
> 個人隨時查詢[5][7]；無薪假需補單並於計薪時自動扣除

**處置：全條照做，無爭議（首條無 C 單尾巴）。** 拆成六個要素後，三成三缺。

#### 現況盤點（實際讀碼）

| 要素 | 現況 | 依據 |
|---|---|---|
| 主管審核簽核 | ✅ 已有 | approval-flows 多關簽核 + approval_steps |
| 核准後自動扣減假數 | ✅ 已有 | `services/ledger.ts` `applyApprovalEffects` → `debitLeaveBalance` |
| 個人隨時查詢 | ✅ 已有 | `GET /leave-balances`，ESS 前端已接 |
| **週年制（依到職日）** | ❌ **現況是曆年制** | 見下方 |
| **特休自動發放（§38 天數表）** | ❌ 完全沒有 | `entitled` 由 HR 手動 `PUT /leave-balances` 設定 |
| **無薪假計薪自動扣除** | ❌ 完全沒有 | 薪資引擎無請假輸入 |

#### 缺口一：餘額桶的 key 是曆年，不是週年（結構問題，非補功能）

```ts
// apps/api/src/services/ledger.ts:41
function requestYear(req: ApprovedRequest): number {
  const y = new Date(req.start_at).getUTCFullYear()   // ← 曆年
  return Number.isFinite(y) ? y : new Date().getUTCFullYear()
}
```

`leave_balances` 的唯一鍵是 `(tenant, employee, leave_type, year)`，`year` 是
整數曆年。**這不是少一個功能，是桶子的 key 錯了。**

具體後果：員工 2024-03-15 到職，週年制的假期年度為 2024-03-15 ~ 2025-03-14。
現況下 2024-12-01 請的假記進 `year=2024`、2025-02-01 請的假記進 `year=2025`
——同一個週年年度被切成兩個桶。**除了 1/1 到職的人，所有人的剩餘假數都是錯的。**

修正方向：`leave_balances` 改以「假期年度期間」為鍵（`period_start` /
`period_end`，或保留 `year` 但語意改為「到職後第 N 年」），
`requestYear()` 改為依 `employees.hire_date` 推算週年期間。
`hire_date` 欄位已存在（`schema/employees.ts:19`），
`seniorityDays()` 也已存在（`routes/employee-profile.ts:117`），原料齊。

> 法規面：勞基法 §38 依「繼續工作期間」計算，**週年制本就是法定原則**；
> 曆年制是施行細則 §24 的變通，須勞工同意且不得損及權益，轉換年度還要
> 比例計算。客戶選週年制是選對的；反而現況的曆年制若未經協商，本身有問題。

#### 缺口二：特休天數表不存在，`entitled` 全靠 HR 手動輸入

§38 天數級距（6個月3日／1年7日／2年10日／3年14日／5年15日／10年起每年加1日
至30日封頂）在 repo 內**完全不存在**。

依本專案硬約束「法規參數一律進 `system_parameters` 帶生效日，不寫死」，
天數表應建成資料而非程式碼。

**連帶（第 4 條未提但必然跟著來，且會出錢）**：
§38 IV 年度終結未休完之日數，雇主應發給工資；經協商得遞延一年
（施行細則 §24-1），遞延後仍未休仍須發工資。
`leave_balances.deferred` 欄位已存在，但**無任何結算邏輯**。

#### 缺口三：無薪假不會扣薪（money bug）

```ts
// packages/rules/src/payroll-engine.ts:64
export function computePayslip(
  days: AttendanceDay[], salary: SalaryStructure,
  rules: RuleConfig, expenses = 0,
): PayslipBreakdown
```

**引擎沒有任何請假／缺勤輸入。** `method='monthly'` 時
`base = round(salary.baseSalary ?? 0)` 是固定月薪。
→ 月薪制員工請 10 天無薪假，薪水一毛不少。

（`by_attendance_days` 法會因當日無工時而自然少算，但 2026-08-25 已決策
本案薪資制度為月薪，實際走的是 `monthly` 路徑。）

此缺口與 2026-08-26 找到的勞健保費 0 元屬同一類「鏈上斷一節」，但更徹底：
那次是讀取端 mapping 沒接，這次是引擎介面根本沒有這個參數。

#### ⚠️ 待確認：「無薪假」語意（與「祭師/技師」同類的失真風險）

此詞在台灣有兩個**法規體系完全不同**的意思：

| 解讀 | 法源 | 性質 |
|---|---|---|
| (a) **事假** | 勞工請假規則 §7：全年不得超過 14 日，期間不給工資 | 例行，照做即可 |
| (b) **減班休息**（俗稱無薪假） | 勞雇協商減少工時；須報地方主管機關備查、月薪制全時工不得低於基本工資、有實施期間限制 | **另一個模組**，且未報備實施即違法 |

「無薪假需**補單**」聽起來像 (a)（事後補請假單）。但這是推測。
若客戶指的是 (b)，系統設計、法遵流程與風險完全不同，**必須先確認**。

註：「補單」可沿用既有代申請路徑（`onBehalfOfEmployeeId`，HR 代員工申請），
不需新機制。

#### 加值點：§38 VI 的書面通知義務，「隨時查詢」滿足不了

§38 VI：雇主應將特休期日及未休日數所發工資數額記載於勞工工資清冊，
並**每年定期以書面通知勞工**。

「開放個人隨時查詢」是被動的，法律要的是**主動通知**。系統既然已掌握資料，
順手產出年度特休通知書即可同時滿足查詢與法定通知——這是客戶沒要求但
應該給的東西。

> 註：原文帶 `[5]` `[7]` 引註編號，與第 3 條的 `[2]` 一致，
> 再次確認為有註腳的整理稿。

### 第 5 條（2026-09-11）

> 薪資計算與電子薪資條發送：薪資核算與放款確認後，系統需一鍵自動生成電子
> 薪資條，並透過 Email 自動寄送給該員工

**處置：做，但寄送方式與啟用時機要改。無 C 單尾巴。**

#### 現況盤點（實際讀碼）

已有（比想像中多）：

| 項目 | 位置 |
|---|---|
| 一鍵批次算薪 | `POST /payroll/run` → upsert `payslips` (`status='draft'`) |
| 核定鎖定 | `POST /payslips/:id/finalize`（draft → finalized，再跑 run 會跳過） |
| 逐項明細結構 | `PayslipBreakdown.lines`（本俸／加班費／夜間加給／全勤／應扣項目） |
| 員工自助查閱 | `apps/web/src/app/ess/payslips/page.tsx` **已存在** |
| Email + LINE 發送管道 | `services/notification-delivery.ts`（Resend API + LINE push **均已接好**） |

缺：

| 缺口 | 說明 |
|---|---|
| **「放款確認」狀態不存在** | 只有 `draft` / `finalized` 兩態。本條講「核算與**放款確認**後」，語意是三態：核算 → 核定 → 放款 |
| **payslip 與 notification 完全沒接** | grep 為空。自動寄送不存在 |
| **無任何 PDF 產生** | repo 內無 pdf/puppeteer/jspdf |
| **無寄送紀錄** | `payslips` 無 `sent_at`，無投遞 log |

#### 法規：電子薪資條明文可行，但有條件

勞基法 §23 I 要求雇主提供工資各項目計算方式明細；施行細則 §14-1 定明細
應含 (1) 議定工資總額 (2) 各項目給付金額 (3) 依法令或勞雇約定**得扣除項目
之金額** (4) 實際發給金額，且得以紙本、**電子資料傳輸方式**或其他
「勞工**可隨時取得及得列印**」之方式為之。

→ 現有 `PayslipBreakdown.lines` 的結構正好對應 (1)~(4)。

**但「可隨時取得」是 Email 做不到的**：信件會被刪、離職後公司信箱關閉、
換信箱即失聯。真正滿足該要件的是 ESS 常駐查閱頁（**已存在**）。
「得列印」則需可列印版面（print CSS 或 PDF）。

#### 設計建議一：Email 不該夾帶薪資明細（個資風險）

照字面「透過 Email 自動寄送」＝把全公司薪資明細以明文批次寄出。風險：

- `employees.email` 一筆打錯 → 整份薪資明細寄給別人，且**收不回來**
- 薪資屬敏感個資，批次寄送一次出錯即群體外洩
- 郵件於對方伺服器與轉發路徑留存

**建議改為：Email／LINE 只寄「您的 X 月薪資單已可查閱」通知 + 連結，
明細在 ESS 登入後看。**

此方案同時更省工：ESS 頁面已存在、`notification-delivery.ts` 已存在，
接起來即可，**連 PDF 都不必做**。若客戶堅持附檔，則寄加密 PDF。

#### 設計建議二（最重要）：自動寄送會讓「雙軌並行」失效

本專案硬約束：「薪資模組必須平行雙軌 2 個月，逐人差異歸零才准切換」。

**自動寄送在雙軌期間絕對不能開啟**：

- 雙軌期間新舊系統數字仍在對，本就預期有差異
- 薪資條一旦寄到員工信箱即**收不回來**
- 寄出一份錯的薪資單＝直接引爆「算錯錢＝信任崩盤」——正是雙軌要防的事

→ **寄送功能必須有 tenant 級開關，預設關閉，雙軌驗證通過才開啟，
且開關變更本身要進稽核。**

#### 設計建議三：別把「寄送」掛在 finalize 上

`POST /payslips/:id/finalize` 目前**單向不可逆**（已 finalized 再呼叫回 409，
無 un-finalize 端點）。若把自動寄送掛在 finalize，該動作就變成
「不可逆 ＋ 對外發送」的複合操作，手滑一次無法挽回。

建議拆三段：`finalize`（核定鎖定）→ `mark_paid`（放款確認）→
`send`（對外發送，需二次確認）。本條原文的「核算與放款確認後」正好支持這個拆法。

#### 連動：第 4 條的缺口三會變成本條的法遵缺口

施行細則 §14-1 要求薪資明細列出「依法令規定或勞雇雙方約定**得扣除項目
之金額**」。目前引擎的 `attendanceDeduction` 是**全勤獎金的遲到階梯扣款**
（`payroll-engine.ts:155-162`），**不是**缺勤／無薪假扣款。

→ 第 4 條缺口三（無薪假不扣薪）未補之前，薪資條會**少列一個法定應列項目**。
兩條要一起做。

### 第 6 條（2026-09-11）

> 歷史獎金對比與折算機制：發放節慶/三節 Cash 獎金時，系統自動帶出該員工
> 「去年同期發放紀錄」供對比[4]；新進未滿一年員工，按到職月數比例（除以 12）
> 自動折算[8]，並保留老闆最終加減金額的裁示調整權[4]

**處置：全條照做，無爭議。三個部分皆為新建，但地基比預期齊。**

#### 用詞觀察（與逐字稿問題相關）

本條的「**Cash** 獎金」指的是**現金發放的三節獎金**，與第 1~3 條的
「C 單／Cash 卡」不是同一件事。這顯示客戶的語彙中「Cash」單純指
**現金給付管道**。

→ 若第 1 條原話的「Cash 卡」也只是「現金給付的紀錄」，那它指向的正是
本專案已提出的 `overtime_settlements`（工時真實 + 給付管道分流），性質合法。
**但第 2 條的「供勞動局檢查」已排除該善意解讀**，故第 1~3 條處置不變。
此觀察僅作為索取逐字稿的又一理由。

#### 現況盤點

| 需求 | 現況 |
|---|---|
| 節慶／三節獎金模組 | ❌ **完全不存在**（`三節`/`節金`/`年終`/`festival` 全域 grep 為空） |
| 專案分潤獎金 | ✅ 已有，但**是另一個東西**：`projects.bonusPool` + `project_members.sharePct/shareAmount` |
| 裁示調整留痕 | ⚠️ **範本已存在**：`project_share_adjustments`（field / oldValue / newValue / reason / changedByEmpId） |
| 歷史同期對比 | ❌ 無獎金發放紀錄表，無期別概念 |
| 未滿一年 ÷12 折算 | ❌ 無 |
| 二代健保補充保費（獎金） | ⚠️ **引擎已有且正確，但沒有全年累計追蹤**（見下） |

#### 缺口一：補充保費算得出來，但沒有累計資料可算

`packages/rules/src/tw-tax.ts:33` `bonusSupplementaryPremium()` 實作正確——
全年累計逾當月投保金額 4 倍者，**只對本次超過門檻的部分**計費（2.11%），
且 `tw-tax.test.ts` 已驗證跨門檻情境。

但唯一的呼叫點是 `apps/api/src/routes/payroll-tax.ts:394`，屬
**手動試算器**：`cumulativeBonusBefore` 由使用者自行填入 request body。

→ **系統沒有任何地方追蹤員工的全年累計獎金。**
三節 3 次 + 年終 1 次，一年至少 4 次發放，每次都要人工記得
「今年到目前為止累計多少」——這必錯，且錯的方向是**漏扣被追繳**。

必須建獎金發放紀錄表（暫名 `bonus_payments`：employee / period / kind /
amount / paid_at），累計由系統算。歷史同期對比也靠同一張表。

> 註：docs/01 已指出的補充保費缺項講的是**複委託（9B 執行業務所得）**，
> 與本條的**員工獎金**是兩個不同款項，兩個都要做。

#### 缺口二：獎金是不是「工資」——客戶沒提，但漏了會在離職爭議爆掉

勞基法施行細則 §10 明列不屬工資之給與，含「年終獎金」與「節金」
（春節／端午／中秋之節金）。→ 原則上不計入平均工資，不影響資遣費、
退休金、加班費基數，亦無須提繳勞退。

**但**若屬經常性給與且與工作有對價（契約明定必發、金額固定、年年照給），
實務上仍可能被認定為工資——此為高頻爭議點。

→ **設計意涵：獎金紀錄必須帶「是否列入平均工資」「是否提繳勞退」兩個旗標**，
預設值與變更理由要留痕。這兩個旗標直接決定資遣費與勞退提繳金額。

#### 缺口三：「÷12 折算」的兩個細節會吵架

1. **「到職月數」定義未明**：未滿一個月算不算？以到職日起算或當月起算？
   月中到職如何處理？——需明確定義並寫進工作規則。
2. **折算需有依據**：若工作規則／契約未載明得按比例折算，員工可主張應全額。
   → 折算比例應為**可設定參數**，且每次發放留存當時的規則版本。

#### 裁示調整權：建議 `reason` 設為必填

「歷史同期對比」一旦做出來，**調降就變得可見且可被質問**——系統會逼老闆
給理由。這是功能的必然副作用。

建議把裁示調整的 `reason` 設為**必填**（現有 `project_share_adjustments.reason`
可空）。這保護的是老闆自己：有理由紀錄，爭議時站得住；沒有理由，
從紀錄上看起來就是任意調降。

比照 `project_share_adjustments` 的欄位結構即可，不需重新設計。

#### 順手發現：`salary_adjustments` 沒記「誰改的」

`packages/db/src/schema/salary-adjustments.ts` 有
effectiveDate / newSalary / reason，但**沒有 `changedByEmpId`**——
同 repo 的 `project_share_adjustments` 有。

→ 調薪紀錄查不到操作者。經手金額的表缺操作者欄位，與硬約束
「經手金額的表一律掛 audit trigger」相違。加一個欄位即可，列入待辦。

### 第 7 條（2026-09-11）

> 行政福利與排班管理：系統需具備生日紅包發放提醒（包含提醒拍照留存）[8][9]，
> 以及值日生與總機接電話輪播排班表[9]

**處置：兩件事都做，無爭議。但值日生輪值「不能」塞進現有 schedules。**

#### 現況：地基意外地齊

| 零件 | 現況 |
|---|---|
| 員工生日欄位 | ✅ `employee_profiles.birthday` (date) 已存在 |
| 排程器（定時觸發） | ✅ BullMQ 已在跑三個：attendance daily、detection daily、**notifications every 5 min**（`apps/worker/src/worker.ts:124`） |
| 通知表 + 發送 | ✅ `notifications`（type/title/body/channel/payload/status/sentAt）+ `notification-delivery.ts`（inapp / LINE / email） |
| 私有檔案儲存 | ✅ Supabase Storage 私有 bucket 已在用（`request_attachments.storagePath`、`employee_profiles.photoStoragePath`、`employee_educations.proofStoragePath`） |
| 生日提醒 | ❌ 無 |
| 紅包發放紀錄 | ❌ 無 |
| 值日生／總機輪值表 | ❌ 無 |

→ 生日提醒是既有 pattern 的複製：加一個 daily scheduler + `notifications.type='birthday'`。
`type` 現有列舉為 missing_punch / anomaly / approval / report / announcement，加一個即可。

#### 值日生輪值：不能用 schedules，這是 DB 擋住的

```ts
// packages/db/src/schema/schedules.ts
tenantEmployeeDateUnique: uniqueIndex("schedules_tenant_employee_date_uq")
  .on(table.tenantId, table.employeeId, table.workDate)
// 註解原文：makes "one schedule per employee per day" a hard DB invariant
```

**一人一天只能有一筆排班，是 DB 硬約束。** 若把值日生／總機輪值塞進
`schedules`，會與工作班別**互斥**——員工當天要嘛有工作班別、要嘛當值日生，
無法兩者兼具。

更嚴重的是結算污染：`settlement.ts` 會取 `schedules.shift_id` →
`shifts.start_time / end_time / break_minutes` 去算工時與遲到。
把「值日生」建成一個 shift，會直接改變該員當日的工時與遲到計算。

→ **必須另建輕量的 `duty_rosters`（職務輪值表：tenant / duty_type /
work_date / employee_id / note）。這不是設計偏好，是唯一索引擋住的。**
輪值不影響工時結算，兩者刻意脫鉤。

#### ⚠️ 輪值的工時陷阱（客戶問排班表時不會想到）

輪值表本身不是工時。**但輪值造成的額外在勤時間是工時**：

- 總機需比同仁早到開機／晚走關機 → 該段為工時
- **午休留守接電話 → 該段為工時**。勞基法 §35 休息時間應得自由利用；
  被要求留守待命者，該時段不算休息，應計入工時並影響加班計算。

這是實務上最常漏、且勞檢會抓的一項。

→ 設計意涵：`duty_rosters` 應可標記「本輪值是否產生額外在勤時間」及其時段，
並由該標記餵進出勤結算，而非讓輪值與工時完全無關。

#### 生日紅包：不能只做提醒，錢要有紀錄

現金給付的生日禮金屬員工所得，須併入申報（公司直接給付者屬薪資所得；
由職工福利委員會以福利金給付者所得類別不同）。少額、非現金、慰勞性質的
實物另有認定，**但現金紅包一律要報**。
※ 所得類別與免稅範圍待會計師確認，依硬約束不在此臆斷。

→ **只做「提醒」而不記錄發放金額，年底扣繳憑單會漏。**

**架構決定：生日紅包與第 6 條的三節獎金共用同一張 `bonus_payments`。**

理由不只是省工，是正確性需求：第 6 條已確認二代健保補充保費以
「**全年累計**獎金逾投保金額 4 倍」計算（`tw-tax.ts:33`）。
生日紅包若記在別處，累計金額就會少算 → 補充保費漏扣 → 被追繳。

`bonus_payments` 以 `kind` 區分（festival / year_end / birthday / project_share…），
並帶第 6 條already 提出的兩個旗標（是否列入平均工資、是否提繳勞退）。

#### 「提醒拍照留存」：要先問拍什麼，且建議以簽收為主

「拍照留存」有三種可能，性質完全不同：

| 拍什麼 | 性質 |
|---|---|
| (a) 紅包本身／發放當下 | 內部憑證，合理 |
| (b) 員工收紅包的合照 | 涉肖像權與個資法 §8 告知、§19 特定目的；存放位置、可見範圍、保存期限都要定 |
| (c) 簽收單 | 等同簽收證明，最直接 |

**更根本的問題是目的**：若目的是證明「錢確實發到員工本人手上」
（防代領／侵占），**照片是弱證據，簽收紀錄才是強證據**。

→ 建議：**ESS 電子簽收為主**（員工自己登入確認收到，有身分、有時間戳、
不可否認），**照片為輔且選填**。既更可靠，也避開 (b) 的個資問題。
儲存沿用現有私有 bucket 模式。

---

## 模組二：行政公告與法務備查（Notice & Contract Compliance）

> 模組一於第 7 條結束（使用者確認無第 8 條）。

### 模組二第 1 條（2026-09-11）

> 紙本簽名公告上傳與備查：公司請假與福利等公告規章，採紙本傳閱全員簽名後，
> 掃描檔案上傳至系統專區供同仁查閱[10]，不採線上單純勾選同意，以防範勞資爭議

**處置：照做。客戶的直覺是對的，但紙本有他沒想到的弱點，系統要補起來。
建議「兩者都留」而非二選一。**

#### 這是第一條客戶主動選擇更保守做法的規則

前七條的模式是客戶想省事、我們指出風險。本條相反：客戶主動放棄線上勾選、
選擇紙本簽名以防勞資爭議。**這個直覺正確，照做。** 以下是把它做對的細節。

#### 法定義務其實不是「簽名」

- 勞基法 §70：僱用 30 人以上者應訂立工作規則，報請主管機關**核備**並**公開揭示**
- 施行細則 §37：工作規則應**揭示於事業場所內顯著之處，或發給勞工**

→ **法律要求的是「揭示或發給」，不是「取得每個人簽名」。**
簽名是雇主自行加上的舉證強化手段，用來證明「該勞工確實知悉」。

客戶的真實需求因此是：**證明某位員工在某時間確實看過某一版規章。**

#### 紙本掃描的三個舉證弱點（客戶未必想到）

1. **掃描後時間資訊消失。** 掃描檔只是一張圖，無可信時間戳。
   員工可主張「簽名是事後補的」「日期由公司代填」。
2. **無法證明簽的是哪一版。** 規章改版後，舊簽名頁對應哪一版？
   無版本綁定即說不清。**而現況更糟——連「版本」這個概念都不存在（見下）。**
3. **傳閱單會遺失，且是單點失效。** 全員簽名集中於一張紙，遺失即全無。

對照之下，設計良好的線上確認舉證力其實**強於**紙本掃描：
登入身分（Supabase Auth 帳號綁定）＋ 伺服器時間戳（不可竄改）＋
**文件版本 hash**（證明看的是哪一版、內容未被替換）＋ IP/device ＋ 稽核軌跡。

**但這不是要推翻客戶的決定。** 客戶要紙本就做紙本；重點是用系統把紙本的
弱點補起來。

#### 建議設計：三層，不是二選一

| 層 | 內容 | 作用 |
|---|---|---|
| 一（法定） | **查閱紀錄**：誰在何時開過這份文件 | 證明「已發給且可取得」，滿足施行細則 §37。**這是被動 log，不是「勾選同意」**，沒有客戶擔心的問題 |
| 二（客戶要的） | **紙本掃描上傳 + 逐人簽收追蹤** | 關鍵：不能只存一個 PDF，要建 per-employee 簽收紀錄，才知道**誰還沒簽** |
| 三（補強紙本） | **版本管理 + 檔案 hash + 生效日** | 證明簽的是哪一版、內容未被替換 |

#### 不利益變更：這才是紙本簽名真正的價值所在

若公告內容涉及**對勞工不利的變更**（減少福利、請假規定變嚴），
單純「揭示」不足。實務見解：不利益變更原則上需**勞工個別同意**，
否則對不同意之勞工不生效力。

→ 規章版本應可標記「本次變更是否涉及勞動條件不利益變更」。
若是，簽收的性質就不是「已閱讀」而是「同意」，且必須**逐人追蹤到齊**。

**客戶做紙本簽名的直覺是對的——但只有在系統幫他管「誰還沒簽」時才有用。**
一張傳閱單傳完，沒人知道少了誰。這是本條最該做的功能。

#### 現況：公告表存在，但對「備查」有三個致命洞

```ts
// packages/db/src/schema/announcements.ts — 全部欄位
title / body / audience / createdBy / createdAt / updatedAt
```

| 洞 | 說明 |
|---|---|
| **無版本管理** | `PATCH /announcements/:id`（`routes/announcements.ts:111`）**就地覆寫**。規章一改，舊版消失。對「備查」是致命的——爭議時無法證明當時公告的內容 |
| **可直接刪除** | `DELETE /announcements/:id`（:155）可刪掉規章，**無留痕** |
| **無附件** | 完全沒有檔案欄位。掃描檔無處可放（但 `request_attachments` 的私有 bucket 模式是現成範本） |
| 無查閱紀錄 | grep `read_at` / `acknowledg` 為空 |
| 無簽收追蹤 | 同上 |

**且 `audit_logs` 在本 repo 完全不存在**（帳本移植缺口 #1）。
誰上傳、誰改生效日、誰刪了舊版——全部查不到。
**公告備查模組正是最需要 audit_logs 的地方**，這條與缺口 #1 應綁在一起做。

#### 動工順序建議

1. 先補 `audit_logs`（移植缺口 #1）——否則本模組做出來仍然「備查」不了
2. `announcements` 加版本：改為 append-only 版本鏈，`PATCH` 改為發新版；
   `DELETE` 改為 soft-delete（archived）並留痕
3. 加附件（沿用 `request_attachments` 私有 bucket 模式）+ 檔案 hash
4. 加 `announcement_acknowledgements`（per-employee 簽收：viewed_at / signed_at /
   scan_file_id / 對應版本），並提供「**誰還沒簽**」的清單視圖
5. 版本加「是否涉及不利益變更」旗標

### 模組二第 2 條（2026-09-11）

> 公告版本控制與進版紀錄：當條款跨年度或有增修時，系統需建立新版號
> （第一版、第二版進版控制）[13][14]；舊版公告及請假單據需永久保留
> 5 至 7 年追溯期，嚴禁系統刪除

**處置：照做，且優先度高於前面所有新功能——因為系統現在正在做客戶
明文禁止的事。**

本條與模組二第 1 條是同一件工程，客戶自己把「版本控制」與「禁止刪除」
明確提出來，證實第 1 條盤點出的洞不是推測的風險。

#### ⚠️ 最嚴重的發現：請假單據現在就可以被硬刪

```ts
// apps/api/src/routes/requests.ts:1028
// DELETE /requests/:id — HR admin removes a non-approved form record.
// Approved records are retained because they may already have ledger/payroll effects.
if (lr.status === "approved") {
  res.status(409).json({ error: "approved_request_cannot_be_deleted" })
}
// 否則連刪三張表：
await supabaseAdmin.from("request_attachments").delete()...
await supabaseAdmin.from("approval_steps").delete()...
await supabaseAdmin.from("leave_requests").delete()...
```

**保護判準錯了。** 現況保護的是「會不會影響帳」，不是「會不會有爭議」：

- `approved` 的請假單 → 擋住（因為有 ledger effect）
- `pending` / `rejected` / `cancelled` → **HR 可硬刪**，連同附件與簽核軌跡一起消失

**而被駁回的申請，恰恰是最可能產生爭議的那一種。**
情境：員工提請假單 → 主管駁回 → 日後爭議，員工主張「我有申請、公司不准」
→ 該筆 rejected 紀錄已被刪 → 證據滅失，且滅失的是對雇主不利時唯一的反證。

這是設計者視角（保護帳務完整性）而非法務視角（保護爭議證據）的結果。

順帶：程式刪了 `request_attachments` 索引列，但**未刪 Supabase Storage 內的
實體檔案** → 產生孤兒檔案。既是儲存成本，也是個資治理問題（檔案仍在但無人管）。

#### 其他硬刪端點（全部無留痕、無 soft-delete）

`announcements:155`、`projects:561`、`leave-types:170`、`kpi-templates:151`、
`onboardings:317`、`employee-profile:472/561`、`recruitment:129`。

`services/tenants.ts:60,77` 的 tenant 刪除屬 provisioning 回滾，性質不同，可接受。

#### 「嚴禁系統刪除」在應用層擋不住——必須做在 DB 層

整個 API 走 `supabaseAdmin`（service_role key），而 **service_role 繞過所有
RLS**。因此：

- 寫 RLS policy 禁止 DELETE → **無效**，service_role 照刪
- 移除 endpoint → 只移除一條路徑，key 在手仍可直接下 DELETE
- 且本 repo 的 `packages/db/sql/*.sql` 共 17 支 RLS 檔，**grep 不到任何
  DELETE 相關 policy**，DB 層目前零保護

**唯一連 service_role 都擋得住的是 BEFORE DELETE trigger**（trigger 不受
RLS bypass 影響）：

```sql
CREATE FUNCTION forbid_delete() RETURNS trigger AS $$
BEGIN RAISE EXCEPTION 'deletion forbidden on %', TG_TABLE_NAME; END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER no_delete BEFORE DELETE ON leave_requests
  FOR EACH ROW EXECUTE FUNCTION forbid_delete();
```

> 此點與帳本待辦 #4（「RLS policy 只在 pglite 以 owner 身分驗過，等於未驗」）
> 是同一個主題：**owner / service_role 繞過 RLS**。本條正好說明
> RLS 不是防刪的正確工具。

**實作注意（會咬人）**：現有測試以 `.delete()` 清理資料
（`payroll-run.test.ts`、`ledger.test.ts`、`kpi.test.ts` 等多處）。
加上 trigger 後這些測試會全部失敗。需先決定清理策略
（獨立 schema / 專用清理角色 / 測試改用 soft-delete 後 truncate），
不能直接加 trigger 了事。

#### 保存年限：不要用單一數字，且起算點客戶沒講清楚

法定年限依文件類型而異：

| 文件 | 年限 | 法源 |
|---|---|---|
| 出勤紀錄 | 5 年 | 勞基法 §30 V |
| 工資清冊 | 5 年 | 勞基法 §23 II |
| 勞工名卡 | 自離職起 5 年 | 勞基法 §7 II |
| 會計憑證 | 5 年 | 商業會計法 §38 |
| 會計帳簿、財務報表 | 10 年 | 商業會計法 §38 |
| 請假單據 | 法無明定 | 實務比照出勤紀錄 |

客戶說的「7 年」可能來自會計端的較長年限。
※ 各年限請會計師／律師覆核，依硬約束不臆斷。

**關鍵：起算點不是「文件建立日」，是「請求權時效屆滿日」。**

工資（含加班費）請求權適用民法 §126 五年時效，自**各期工資給付日**起算；
退休金請求權 5 年（勞基法 §58）。一份 2026 年的請假單，若員工 2026 年離職，
爭議可能 2031 年才發生。

**對規章版本更關鍵**：某一版規章要保留到
**「最後一位受該版規範的員工」請求權時效屆滿**，
而非「該版失效後 5 年」——因為爭議時要證明的是「當時適用哪一版」。

→ **保存期限應掛「最後適用日 + N 年」，不是「建立日 + N 年」。**
起算點取錯會提早刪掉關鍵證據。

**建議：根本不做自動清除機制。** 客戶說「嚴禁系統刪除」，那就不要寫 purge。
保存期限僅用於標示「可歸檔／可降冷儲存」，不觸發刪除。
儲存成本遠低於證據滅失的代價。

#### 版本模型

客戶講的「第一版、第二版」是**人類可讀版號**，與內部版本鏈是兩件事：

- `version_no` int — 給人看的版號
- `effective_from` / `effective_to` — 該版適用期間
- `supersedes` / `superseded_by` — 版本鏈
- `content_hash` — 證明內容未被替換
- `change_type` — **`'amendment'`（增修）vs `'annual_rollover'`（跨年度）**

最後一項對應客戶原文「當條款**跨年度**或有**增修**時」——**兩種觸發條件**。
跨年度意味著即使條款一字未改也要進版（年度特休、年度福利額度這類條款，
適用年度不同即為不同版本）。兩者必須能分開記，否則「今年與去年相同」
這件事本身無法證明。

#### 一致性問題：系統對「不可變資料」沒有統一策略

`payslips` 已有 `status='finalized'` 鎖定 + `version` 欄位遞增；
`announcements` 兩者皆無，`PATCH` 直接就地覆寫。
建議把「鎖定後不可改、只能發新版」做成跨表一致的 pattern，而非逐表各做各的。

#### 2026-09-11 實作：請假單據改軟刪除（模組二第 2 條的最小止血）

**動機**：模組二第 2 條盤點發現 `DELETE /requests/:id` 會實體刪除
pending/rejected/cancelled 的請假單，連同附件與簽核軌跡。客戶明文
「嚴禁系統刪除」，而系統正在刪的恰是最會產生爭議的那一種（被駁回的申請）。
資料刪掉回不來，故優先於所有新功能先做。

**改動**

| 檔案 | 內容 |
|---|---|
| `packages/db/src/schema/leave-requests.ts` | 加 `deletedAt` / `deletedByEmpId` / `deleteReason` 三欄（皆可空，null = 未註銷） |
| `packages/db/migrations/0026_flashy_moondragon.sql` | 對應 migration（drizzle-kit generate） |
| `apps/api/src/routes/requests.ts` | DELETE 端點改軟刪除；**不再連坐刪除 request_attachments 與 approval_steps**；`reason` 必填；已註銷回 409 `already_deleted`；已核准仍回 409。2 個列表 + 5 個動作載入加 `deleted_at IS NULL` 過濾 |
| `apps/api/src/services/ai-insights.ts` | 分析查詢加同一過濾 |
| `apps/web/src/lib/admin-api.ts` | `deleteRequest(id, reason)` 帶理由 |
| `apps/web/src/app/admin/form-records/page.tsx` | 改 prompt 收理由；「刪除」字樣改「註銷」 |

**保留的硬刪除**：`requests.ts:275` 建單時 approval_steps 插入失敗的回滾。
該列剛建立、無簽核軌跡、非證據，清掉孤兒列正確。

**設計取捨**

- 原狀態（rejected / cancelled）**不被覆寫**——「被駁回」本身是證據。
- 已核准者維持不可註銷：其 ledger 效果（假別餘額）已發生，註銷會造成
  餘額與單據不一致。
- `reason` 必填：無理由的註銷正是本機制要防的事（同模組二第 1 條與
  第 6 條對 `reason` 的主張）。

**驗證**：typecheck 6/6、rules 49、**db 12 → 15**（新增 3 項 schema 測試）、
build 4/4。api 新增 8 項整合測試（缺 Supabase 憑證，本機不可跑，
基準線 18 檔失敗不變）。

**尚未做（需先決策）**：DB 層 `BEFORE DELETE` trigger。
應用層擋不住持 service_role key 者直接下 DELETE，但加上 trigger 會使
現有多支測試的 `.delete()` 清理全數失敗，需先定清理策略
（獨立 schema／專用清理角色／改 truncate）。**這仍是真正的防線，未完成。**

其餘硬刪端點（announcements、projects、leave-types、kpi-templates、
onboardings、employee-profile、recruitment）本輪未動，待模組二正式動工時
一併處理。
