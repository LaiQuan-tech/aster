# 亞斯特 ERP — 專案帳本

> 本 session 無 Task* 工具，帳本以檔案維護。每次 session 開場先讀這份。
> 建檔：2026-08-24

## 決策紀錄

| 日期 | 決策 | 理由 |
|---|---|---|
| 2026-08-24 | 自行開發（非外包規格書） | 使用者裁示 |
| 2026-08-24 | 資料模型先行、實作分階段 | 專案/請款/獎金是同一組資料的三個投影，切碎做會重工 |
| 2026-08-24 | **不實作 C 單拆卡機制** | 偽造出勤紀錄；系統化後留下完整證據鏈，風險高於現行人工作業。改以「工時預警 + 紀錄真實/給付管道分流」覆蓋需求 |
| 2026-08-24 | 薪資模組採平行雙軌 2 個月才切換 | 算錯錢＝信任崩盤，屬 judgment.md 第1條 (a) 靜默污染 |
| 2026-08-24 | 跨公司金流＝**集團代收代付**（使用者確認） | 據此加建區塊 L：往來款流水、沖抵、帳齡 view |
| 2026-08-24 | 開票公司 ≠ 簽約公司時「標記不阻擋」 | 該情況已非代收代付；不擋流程（實務有合理情境），但要在報表可見 |
| 2026-08-25 | 薪資制度＝**月薪 + 外包承攬**（使用者更正，非時薪） | 薪資引擎只需一條月薪路徑；外包走 9B 執行業務所得 |
| 2026-08-25 | **承攬人員不進打卡/請假/薪資模組** | 出勤紀錄是「受指揮監督」的證據，會成為假承攬真僱傭的證物 |
| 2026-08-25 | 技術棧：Vite 6 + React 19 + TS + Supabase + Tailwind 4 | 延續 mazu-beta 慣例；新增 Tailwind（ERP 表單密集）與 router |
| 2026-08-25 | 核心計算寫在 PostgreSQL function，不寫在前端 | 可搬移到任何 PG（含公司內網自架），Supabase 只當 auth + hosting |
| 2026-08-25 | 驗證用 pglite（WASM PG），不依賴 Docker | 本機無 Docker/postgres；migration 與引擎皆可實跑驗證 |

## 進度

- [x] 需求評估、指出規劃書失真點（語音轉錄「技師→祭師」）
- [x] 資料模型 v0（docs/02-資料模型-v0.md）
- [x] 關係企業性質確認（集團代收代付）→ 區塊 L 已建
- [ ] **取得會議逐字稿** ← 目前最大阻塞，見 01 檔待確認清單
- [ ] 資料模型 v1（逐字稿校正後）
- [x] 技術棧決策（Supabase + PG function，保持可搬移到內網）
- [x] **DB 層完成**：37 張表 / 32 個 function，40 項功能測試通過
  - [x] 出勤打卡（GPS 圍欄、補登審核、工時分段）
  - [x] 請假特休（週年制自動發放、餘額流水帳）
  - [x] 薪資引擎（月薪、加班費、勞健保、平行雙軌）
  - [x] 外包承攬給付（9B 扣繳起扣點、補充保費）
  - [x] 獎金（批次、比例分配、裁示調整留痕、歷史對比）
  - [x] 稽核軌跡與 RLS 權限分層
- [ ] **法規參數查證**（會計核對）＋兩張表匯入：投保級距、扣繳稅額表 ← 阻塞上線
- [ ] 前端：手機打卡 PWA
- [ ] 前端：補登審核、請假簽核
- [ ] 前端：薪資試算與雙軌比對
- [ ] 前端：獎金分配與發放
- [ ] 專案/請款/獎金連動模組（客製化階段）

## ⚠️ 2026-08-26 重大轉向：改以既有 repo 為基礎

使用者提供 https://github.com/LaiQuan-tech/aster —— 經檢視，**這不是範例，
是已經在為亞斯特開發中的實際系統**（2026-07-27 commit 將系統名改為
「亞斯特設計顧問 數位化系統」，8/19 仍在補 RLS），已部署 Vercel + Railway。

規模：41 張表、40,048 行 TS/SQL、25 個測試檔、26 個 migration。
技術棧：Turborepo monorepo / Next.js 16 + Express 5 + BullMQ / Drizzle + Supabase RLS。

**決策：以該 repo 為開發基礎。本專案（aster-erp）的 migration 停止開發。**
理由：對方覆蓋更廣（多排班、簽核流程、KPI、招募、通知）、引擎品質更高
（規則驅動純函式、golden test 已對過客戶既有薪資表）、且已上線。
本專案的 SQL 是平行造輪子。

### 本專案仍有效、應移植過去的部分

| 項目 | 對方狀態 | 來源 |
|---|---|---|
| 稽核軌跡 audit_logs | **完全沒有** | 008_audit_rls.sql |
| 關係企業代收代付 + 帳齡 | **0 個檔案** | docs/02 區塊 L |
| 分期請款 / 發票 / 入帳 | **0 個檔案** | docs/02 區塊 F |
| 合約版本 + 印花稅追蹤 | **0 個檔案** | docs/02 區塊 E |
| 雇主負擔成本（勞退6%、雇主勞健保） | 引擎未計算 | 006_payroll_engine.sql |

docs/01（C 單否決理由、代收代付邊界）與 docs/02（模型分析）不受影響，仍為有效分析。

### 在對方程式碼發現的疑似 bug（待確認）

`packages/rules/src/payroll-engine.ts:191` 計算健保自付額時用
`(1 + (salary.nhiDependents ?? 0))`，**未裁切眷屬 3 口上限**；
同 repo 的 `tw-tax.ts:108` `nhiEmployeePremium()` 有正確裁切，且其測試
明確驗證 5 口應以 3 口計。兩條路徑邏輯不一致，實際算薪資單走的是前者。
若寫入 nhiDependents 前未於他處裁切，眷屬 4 口以上員工每月會被多扣健保費
（30,000 投保、5 口 → 多扣約 930 元/月）。
※ 未完整追查 API 層寫入路徑，需確認後再定論。

## 2026-08-26 aster repo 實跑狀態

已 clone 至 `~/Documents/coding/aster`（完整 clone，可推送）。

| 項目 | 結果 |
|---|---|
| npm install | ✓ 成功（14 個 vulnerabilities 待評估） |
| typecheck | ✓ 5/5 全過（修 turbo 依賴後 clean clone 可直接跑） |
| @hr/rules 測試 | ✓ 41 項全過（golden 23 + tw-tax 14 + rules-schema 4） |
| @hr/db 測試 | ✓ 12 項全過 |
| @hr/api 測試 | ✗ 20 檔載入失敗——整合測試需真實 Supabase 憑證 |
| web 前端啟動 | ✓ 登入頁正常渲染（Next.js 16.2.9 Turbopack） |
| 登入看系統內部 | ✗ 需真實憑證；demo 帳號亦走 Supabase Auth |

### 本輪修掉的三個問題（皆影響新環境，尚未 commit）

1. **turbo.json 缺 `dependsOn: ["^build"]`**（test/typecheck）
   → clean clone 跑 typecheck 會噴 10 個 `Cannot find module '@hr/rules'`，
     因為 rules 的型別在 dist/。CI 或新開發者必踩。
2. **apps/web 未載入 repo 根 .env**
   → apps/api 與 apps/worker 都有用 dotenv 載根目錄，只有 web 漏了。
     線上靠 Vercel 注入環境變數所以沒現形，本機開發則前端直接白畫面
     （client 端 `supabaseUrl is required`）。已於 next.config.ts 補上，
     並用 `process.cwd()` 而非 `import.meta.url`——next.config.ts 會被編成 CJS。
3. **缺 .env.example**（.gitignore 原已預留 `!.env.example` 例外，但檔案不存在）
   → 已補，含全部 24 個環境變數與用途說明。

### 阻塞：需要真實 Supabase 憑證

`~/Documents/coding/aster/.env` 目前是 placeholder。需由使用者自行填入真實值
（**不要貼進對話**，service_role key 可繞過 RLS）。

## 2026-08-26 重新部署上線

README 指的兩個網址都是錯的：`hr-theta-peach`（更名前 HRLink 版本）與
Railway API（服務已不存在，404）。實際專案是 Vercel 上的 aster-system / aster-hr-api，
且**最近一次部署 15 天前為 Canceled，線上實跑的是 26 天前（約 7/31）的版本**，
缺 8/07 薪資引擎修正與 8/19 RLS 資安修補。

已從本地重新部署兩者至 production：

- Web: https://aster-system.vercel.app
- API: https://aster-hr-api.vercel.app/health → `{"status":"ok"}`
- Supabase: jpbtwvxwjrjspkiinrvy.supabase.co（存活，回 401 要 apikey 屬正常）
- 前端 NEXT_PUBLIC_API_URL 已正確指向 aster-hr-api.vercel.app

部署內容 = main HEAD + 下列未 commit 的本地修改（線上與 git 暫不一致，待 commit）：
`turbo.json`、`apps/web/next.config.ts`、`apps/web/package.json`、
`.env.example`(新)、`.gitignore`(加 .vercel)、`README.md`(線上環境網址更正)。

## 2026-08-26 找到並修正薪資 bug（比原判斷嚴重）

原本懷疑的是「健保眷屬未裁切 3 口上限 → 多扣」，追下去發現是更根本的斷鏈：

`apps/api/src/routes/payroll.ts` 的 `SalaryRow` / `toSalaryStructure` 只映射
method / baseSalary / dailyWage / hourlyWage，**未帶入 labor_insured_salary 與
health_insured_salary**。引擎的保費計算是 `ins && salary.laborInsuredSalary ? ... : 0`，
欄位缺漏時靜默走 0 → **每張薪資單的勞保費、健保費都是 0，實發金額被高估**。

鏈上其他環節都在（寫入端 routes/salary.ts、DB 欄位、引擎計算、rule_config 的
insurance 設定），只有讀取端 mapping 沒接上。以投保 38,200 計，每人每月少扣
955 + 592 = 1,547 元。眷屬則是完全沒計入。

### 修正（commit e01516b, d25f9dc）
- payroll.ts: 補兩個投保薪資欄位的 select 與映射
- payroll.ts: 查 nhi_dependents（insured=true）算眷口數帶入引擎
- payroll.ts: 回傳 `missingInsuredSalary`，列出有保費規則卻未填投保薪資者
- payroll-engine.ts: 健保改走 tw-tax 的 `nhiEmployeePremium`，法定 3 口上限
  裁切邏輯只留一份（原先兩處實作不一致）
- 新增 payroll-engine.test.ts 8 項回歸測試

驗證：typecheck 6/6、rules 49 項（原 41 + 新 8）、db 12 項全過、build 4/4。
API 已部署 production，health 回 ok。已 push（2026-09-10）。

### 仍存在的缺口
`salary_structures` 無勞退自提率與預支欄位 → `pensionVoluntaryRate` /
`advance` 仍無資料來源，需另加 migration。

## 實跑驗證紀錄

- 2026-08-25 `npm test`：8 個 migration 全通過，40 項功能測試全通過。
  驗證環境為 pglite（WASM PostgreSQL），非 Supabase 實機——
  RLS policy 的實際行為需在 Supabase 上以真實 anon/authenticated 身分再驗一次。

## 未決事項

見 `01-需求評估與待確認.md` 的待確認清單（10 項）。

---

## 2026-09-10 換機器交接

程式碼與文件全部在 GitHub（`LaiQuan-tech/aster`，main）。原 `aster-erp/`
目錄不在任何 repo 底下，內容已收進本 repo `docs/設計參考/`。

**新 session 接手順序**：本檔 → `01-需求評估與待確認.md` → `aster/README.md`。

### 待辦（依急迫度）

1. ~~**repo 是 public**~~ **已完成（2026-09-11 確認 visibility=private）。**
   金鑰未曾進過 git（全歷史掃過，只有 .env.example 佔位符），毋須輪換。
   ⚠️ 但同組織下 `LaiQuan-tech/aster-pitch` 仍為 public，內容未查，需確認
   是否含本專案資料。
2. **dependabot 29 個漏洞**，4 個 critical（next、vitest）。上線前要處理。
3. `salary_structures` 缺勞退自提率、預支欄位 → 需 migration。
4. RLS policy 只在 pglite 以 owner 身分驗過，等於未驗；需以真實
   anon/authenticated 身分在 Supabase 重驗。
5. 查 Vercel Git integration 為何停止追蹤 main（8/19 的 RLS 修正曾兩週未上線）。
6. 移植五個缺口：audit_logs、集團代收代付、應收開票/收款、合約版本與印花稅、
   雇主成本試算。

### 客戶 25 條開發規則

使用者將依逐字稿拆成 25 條規則逐條討論。第 1 條（2026-09-10）：

> 打卡上限與拆單邏輯：正常出勤卡累計滿 40 小時需自動鎖定/阻擋。超過 40 小時
> 之加班或在家工作時數，系統需自動拆單並轉入隱藏版 Cash 卡（C 單/C 版）紀錄

**處置：拆兩半。**
- 前半（40 小時上限與告警）**做**。§32 月加班上限 46 小時（工會同意可 54，
  每三個月 138 封頂），40 當內部緩衝合理。做成 `work_hour_alerts`，
  36/40/46 三段告警，擋的是再派新工，不是擋紀錄。
- 後半（自動拆單轉隱藏 C 卡）**不做**。理由見
  `01-需求評估與待確認.md`。補充論點：若 40 是每週正常工時（§30 的數字），
  拆完後全公司出勤紀錄都會停在剛好 40 小時、零加班——這種整齊本身就是紅旗。
- 替代方案（已在模型內）：出勤照實記 + `overtime_settlements` 獨立給付紀錄，
  可含現金管道與自己的稅務處理。帳面誠實、錢照付。

**逐字稿仍未取得。** 使用者表示 25 條是依逐字稿拆的，已再次請求原文，
特別是第 1 條——「隱藏版 Cash 卡」是整理後的說法，若原話指的是外包／
非僱傭關係的獨立給付管道，性質完全不同，是可以正常做的。

### 第 2 條（2026-09-11）

> 三階段打卡紀錄機制：系統需支援同天多次打卡邏輯，第一與第二次打卡為正常
> 上下班紀錄（供勞動局檢查）；第三次或超額打卡自動隱藏並將數據帶入 C 單系統中

**處置：同樣拆兩半，但前半「已經做完」，後半的否決理由比第 1 條強。**

#### 前半（同天多次打卡）— 不是新需求，現況已支援

本輪實際讀碼確認，非推測：

- `apps/api/src/routes/punch.ts`：`POST /punch` 對單日打卡次數**無任何上限**。
  省略 `type` 時依「今日最後一筆 in/out」推斷，第 3 刷自然回到 `in`。
- `apps/api/src/services/settlement.ts` `pairPunches()`：依時間排序走訪當日
  **全部**打卡，逐對配成 in/out 區間（4 刷→2 段、6 刷→3 段），
  落單的計入 `anomaly.unpairedPunches`。
- `packages/rules/src/worktime-engine.ts` `computeAttendanceDay()`：
  簽章即 `PunchPair | PunchPair[]`，多段相加，註解寫明
  「一個 in/out 對,或多段 (含中離)」。

#### 後半（第 3 刷以後自動隱藏 + 帶入 C 單）— 不做

第 1 條寫「隱藏版 Cash 卡」，字面上還能解讀成給付管道。第 2 條寫明
「第一與第二次打卡為正常上下班紀錄（**供勞動局檢查**）」——
指定了觀眾（勞動檢查員）與動作（自動隱藏）。這不是給付管道的問題，
是對主管機關呈現不實紀錄。落在硬約束第 1 條「不實作出勤紀錄造假機制」，
不再逐案評估。

第 1 條已寫的理由不重複，本條補三點：

1. **本條與第 1 條的判準互斥，同時實作會打架。**
   第 1 條藏的是「累計逾 40 小時的部分」（時數門檻）；本條藏的是
   「第 3 刷以後」（次數門檻）。同一天可以滿足其一而不滿足其二，
   兩套隱藏規則疊在同一批 `punch_records` 上會產生互相矛盾的「可見紀錄」。
   這種不一致本身就是轉錄稿被逐字當規格的徵兆。

2. **兩刷上限會把正常工時也藏掉，方向是反的。**
   午休外出是 4 刷（09:00 in／12:00 out／13:00 in／18:00 out）。
   只留前兩刷，可見紀錄變成「09:00–12:00，當日 3 小時」。
   這不是藏加班，是把下午整段**正常工時**藏掉——對勞檢反而成為
   工資未全額給付的證據，對員工是少算薪。外出洽公、跨日班同理。
   想藏加班的需求，用次數門檻在技術上根本達不到。

3. **「隱藏」在資料庫裡不是刪除，藏不掉。**
   自動隱藏＝加旗標或搬另一張表，原始 `punch_records` 仍在（且本表設計
   為 append-only）。勞動檢查與訴訟中的文書提出命令都調得到；
   而拆單規則與 C 單對照表本身會證明這是設計出來的，不是漏記。

#### 替代方案：顯示分層，不是紀錄隱藏

推測真實痛點是「HR 看報表時中離／外出的打卡把畫面弄亂」。若是如此，
要的是預設視圖收斂，不是紀錄隱藏：

- 報表預設一行摘要「當日首刷／末刷／合計工時／段數」，展開才看全部打卡明細。
  紀錄 100% 完整、逐日至分鐘，勞檢時展開即可。
- 若真實需求是加班費走現金：走第 1 條已定案的 `overtime_settlements`。

**待確認（同第 1 條，仍缺逐字稿）**：「供勞動局檢查」是客戶原話還是整理稿
的推論？若原話只是「報表預設只看上下班兩刷」，那本條前後半都沒有爭議，
純粹是 UI 需求。

#### 本輪在程式碼發現的三個缺口

1. **`break_in` / `break_out` / `outing_in` / `outing_out` 四種打卡形同虛設。**
   `punch.ts` 的 zod schema 收得進去、`punch_records` 存得下，但
   `pairPunches()` 只認 `in`/`out`，其餘型別靜默略過（不配對也不計入 unpaired）。
   工時扣除走的是班別上的固定 `shift.breakMinutes`
   （`worktime-engine.ts`：`workedMinutes = grossMinutes - shift.breakMinutes`）。
   → **外出 2 小時的人，工時不會變少。**
   這是「同天多次打卡」目前唯一真正沒接上的一段，也是本條前半唯一該做的工。
2. **多刷路徑無回歸測試。** `punch.test.ts` 只測到第 1、2 刷與權限隔離，
   沒有第 3 刷以後、也沒有多段結算的測試。
3. **第 1 條的替代方案尚未動工。** `work_hour_alerts` 與 `overtime_settlements`
   在本 repo **不存在**，只在 `docs/設計參考/migrations-superseded/002_attendance.sql`
   （已停止開發的 aster-erp）。

> 法條引用（勞基法 §30 V/VI 逐日記載至分鐘、保存 5 年）沿用 docs/01 既有分析；
> 舉證責任與罰則的確切條號金額待律師／會計師覆核，不在此臆斷。

### 第 3 條（2026-09-11）

> 在家工作（WFH/加做）彈性申報：提供「在家工作/加做」勾選與申請欄位，
> 開放同仁自主申報[2]；加做時數扣除後未超標按正常計算，超標部分自動轉 C 單處理

**處置：前半做（三條裡第一個真正該開發的），後半不做。**

> 註：原文「自主申報**[2]**」帶引註編號，再次確認手上是**有註腳的整理稿**，
> 非逐字稿。該 `[2]` 指向的來源值得索取。

#### 前半（WFH 自主申報）— 要做，且地基已齊

現況：全 repo 無 `wfh` / `remote` / 「在家」任何字樣，這個概念不存在。
但申請簽核的地基剛好完整：

- `apps/api/src/routes/requests.ts`：`KINDS = ["leave","ot","fix_punch","business_trip"]`，
  已有多關簽核（approval-flows）、多段日期（`segments`，最多 31 段）、
  代申請（`onBehalfOfEmployeeId`）、附件（request_attachments）。
- `business_trip` 是現成範本：走同一條簽核管線，但**不動帳**
  （`services/ledger.ts` 的 `applyApprovalEffects` 只處理 `leave` / `ot`），
  核准即視為已授權。

→ 新增 `wfh` kind 是小工，不是新系統。

**但 WFH 與 `business_trip` 有一處關鍵差異：WFH 時數必須動帳。**
在家工作的時數就是工時，要進同一張 `attendance_days`，
不能像 `business_trip` 那樣只標記核准。建議做法：核准後產生
`source='wfh'` 的打卡對餵進 settlement，與現場打卡走同一條結算路徑。

**自主申報必須配核定關卡——這是保護雇主，不是限制。**
若「開放自主申報」沒有核定，風險方向與客戶擔心的相反：員工自報 100 小時
加班，雇主須照付。加班的前提是雇主同意／指派。既有 approval-flows 直接套用。
另：勞動部《勞工在事業場所外工作時間指導原則》對遠距工作即採
「勞工自行記載＋雇主記載」，**自主申報是法定建議做法**，不是灰色地帶。

#### 後半（超標自動轉 C 單）— 不做

同第 1、2 條，理由不重複（此為 C 單尾巴第三次出現，見下方「流程調整」）。
本條補兩點新論據：

1. **這會讓 §32 月上限依設計永遠不觸發，使第 1 條的告警系統失效。**
   第 1 條處置要做 `work_hour_alerts`（36/40/46 三段）。若超標時數自動轉入
   另一個池子，46 小時這條線**結構上永遠達不到**——告警變成裝飾品。
   同一模組內的兩個功能互相抵銷。
   ※ 本輪另確認：`packages/rules/src/rules-schema.ts` 的 `OvertimeSchema`
   僅 `{ rules: OvertimeRuleSchema[] }`（條件式倍率），**整個規則引擎沒有
   任何月上限維度**。§32 的 46/54/138 目前不存在。第 1 條的缺口比原記載更大：
   不只 `work_hour_alerts` 表未建，是 schema 本身缺這個維度。

2. **在家工作發生職災仍屬職災。** 時數未記載，職災認定與勞保給付即生爭議，
   而爭議時舉證不利者為雇主。藏 WFH 時數，正好在最需要紀錄的場景
   拆掉雇主自己的防線。

**替代方案**：WFH 時數照實進 `attendance_days`；超標部分走第 1 條已定案的
`overtime_settlements`（可含現金管道與獨立稅務處理）。與第 1 條同一個答案。

#### 待確認（阻塞資料模型，需逐字稿或問客戶）

1. **「加做」是什麼？三種可能，資料模型完全不同：**
   - (a) 加班的另一種說法 → 就是既有 `ot`，不需新東西
   - (b) 額外承接的專案工作量（設計顧問業常見）→ 屬專案／請款模組，非出勤
   - (c) 為關係企業做的工 → 踩到區塊 L 代收代付＋假承攬，完全不同題目
2. **「扣除後未超標」的「扣除」扣什麼？** 扣休息時間？扣已達正常工時的部分？
   原文未言明。
3. `[2]` 註腳指向的原始來源。

#### 可立即動工的範圍（待使用者確認）

`wfh` request kind：requests.ts 加 KIND、核准後產生 `source='wfh'` 打卡對、
settlement 照常結算、punch_records 的 source enum 加 `wfh`、對應測試。
不含「加做」——語意未定前不建模。

---

## 流程調整（2026-09-11）

C 單尾巴已在第 1、2、3 條連續出現，判準各不相同（時數門檻／次數門檻／
超標門檻）且互相矛盾。**這是同一個決策被整理稿拆成多條重述，不是三個需求。**

後續規則若再出現「自動轉 C 單／隱藏／拆單」尾巴，**直接引用第 1 條處置，
不再重寫否決理由**，把討論時間集中在各條的前半實質需求。
若某條的尾巴出現真正新的性質（例如明確指向非僱傭關係的獨立給付管道），
再單獨評估。

### 第 4 條（2026-09-11）

> 特休與請假自動扣減（週年制）：請假與特休採「週年制」（依員工到職日計算）[7]。
> 同仁於系統提出請假申請經主管審核（approve）後，系統自動扣減剩餘假數並開放
> 個人隨時查詢[5][7]；無薪假需補單並於計薪時自動扣除

**處置：全條照做，無爭議（首條無 C 單尾巴）。** 拆成六個要素後，三成三缺。

#### 現況盤點（實際讀碼）

| 要素 | 現況 | 依據 |
|---|---|---|
| 主管審核簽核 | ✅ 已有 | approval-flows 多關簽核 + approval_steps |
| 核准後自動扣減假數 | ✅ 已有 | `services/ledger.ts` `applyApprovalEffects` → `debitLeaveBalance` |
| 個人隨時查詢 | ✅ 已有 | `GET /leave-balances`，ESS 前端已接 |
| **週年制（依到職日）** | ❌ **現況是曆年制** | 見下方 |
| **特休自動發放（§38 天數表）** | ❌ 完全沒有 | `entitled` 由 HR 手動 `PUT /leave-balances` 設定 |
| **無薪假計薪自動扣除** | ❌ 完全沒有 | 薪資引擎無請假輸入 |

#### 缺口一：餘額桶的 key 是曆年，不是週年（結構問題，非補功能）

```ts
// apps/api/src/services/ledger.ts:41
function requestYear(req: ApprovedRequest): number {
  const y = new Date(req.start_at).getUTCFullYear()   // ← 曆年
  return Number.isFinite(y) ? y : new Date().getUTCFullYear()
}
```

`leave_balances` 的唯一鍵是 `(tenant, employee, leave_type, year)`，`year` 是
整數曆年。**這不是少一個功能，是桶子的 key 錯了。**

具體後果：員工 2024-03-15 到職，週年制的假期年度為 2024-03-15 ~ 2025-03-14。
現況下 2024-12-01 請的假記進 `year=2024`、2025-02-01 請的假記進 `year=2025`
——同一個週年年度被切成兩個桶。**除了 1/1 到職的人，所有人的剩餘假數都是錯的。**

修正方向：`leave_balances` 改以「假期年度期間」為鍵（`period_start` /
`period_end`，或保留 `year` 但語意改為「到職後第 N 年」），
`requestYear()` 改為依 `employees.hire_date` 推算週年期間。
`hire_date` 欄位已存在（`schema/employees.ts:19`），
`seniorityDays()` 也已存在（`routes/employee-profile.ts:117`），原料齊。

> 法規面：勞基法 §38 依「繼續工作期間」計算，**週年制本就是法定原則**；
> 曆年制是施行細則 §24 的變通，須勞工同意且不得損及權益，轉換年度還要
> 比例計算。客戶選週年制是選對的；反而現況的曆年制若未經協商，本身有問題。

#### 缺口二：特休天數表不存在，`entitled` 全靠 HR 手動輸入

§38 天數級距（6個月3日／1年7日／2年10日／3年14日／5年15日／10年起每年加1日
至30日封頂）在 repo 內**完全不存在**。

依本專案硬約束「法規參數一律進 `system_parameters` 帶生效日，不寫死」，
天數表應建成資料而非程式碼。

**連帶（第 4 條未提但必然跟著來，且會出錢）**：
§38 IV 年度終結未休完之日數，雇主應發給工資；經協商得遞延一年
（施行細則 §24-1），遞延後仍未休仍須發工資。
`leave_balances.deferred` 欄位已存在，但**無任何結算邏輯**。

#### 缺口三：無薪假不會扣薪（money bug）

```ts
// packages/rules/src/payroll-engine.ts:64
export function computePayslip(
  days: AttendanceDay[], salary: SalaryStructure,
  rules: RuleConfig, expenses = 0,
): PayslipBreakdown
```

**引擎沒有任何請假／缺勤輸入。** `method='monthly'` 時
`base = round(salary.baseSalary ?? 0)` 是固定月薪。
→ 月薪制員工請 10 天無薪假，薪水一毛不少。

（`by_attendance_days` 法會因當日無工時而自然少算，但 2026-08-25 已決策
本案薪資制度為月薪，實際走的是 `monthly` 路徑。）

此缺口與 2026-08-26 找到的勞健保費 0 元屬同一類「鏈上斷一節」，但更徹底：
那次是讀取端 mapping 沒接，這次是引擎介面根本沒有這個參數。

#### ⚠️ 待確認：「無薪假」語意（與「祭師/技師」同類的失真風險）

此詞在台灣有兩個**法規體系完全不同**的意思：

| 解讀 | 法源 | 性質 |
|---|---|---|
| (a) **事假** | 勞工請假規則 §7：全年不得超過 14 日，期間不給工資 | 例行，照做即可 |
| (b) **減班休息**（俗稱無薪假） | 勞雇協商減少工時；須報地方主管機關備查、月薪制全時工不得低於基本工資、有實施期間限制 | **另一個模組**，且未報備實施即違法 |

「無薪假需**補單**」聽起來像 (a)（事後補請假單）。但這是推測。
若客戶指的是 (b)，系統設計、法遵流程與風險完全不同，**必須先確認**。

註：「補單」可沿用既有代申請路徑（`onBehalfOfEmployeeId`，HR 代員工申請），
不需新機制。

#### 加值點：§38 VI 的書面通知義務，「隨時查詢」滿足不了

§38 VI：雇主應將特休期日及未休日數所發工資數額記載於勞工工資清冊，
並**每年定期以書面通知勞工**。

「開放個人隨時查詢」是被動的，法律要的是**主動通知**。系統既然已掌握資料，
順手產出年度特休通知書即可同時滿足查詢與法定通知——這是客戶沒要求但
應該給的東西。

> 註：原文帶 `[5]` `[7]` 引註編號，與第 3 條的 `[2]` 一致，
> 再次確認為有註腳的整理稿。
